# 复制为 deploy/.env 并填真值(gitignore, 不入库, git pull 不会覆盖)。
# docker compose 在 deploy/ 下运行时自动读取本文件。

# 网关内部共享密钥(agent 与 webui 之间), 改成自定义随机串, 例如: openssl rand -hex 16
GATEWAY_KEY=CHANGE_ME_gateway_shared_key
