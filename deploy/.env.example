# 网关密钥保存在 deploy/secrets/gateway_key；Docker Compose 不再从
# deploy/.env 注入它，避免密钥出现在容器环境变量与 `docker inspect` 输出中。
#
# 创建方式：
#   umask 077
#   openssl rand -hex 32 > deploy/secrets/gateway_key
#   chmod 600 deploy/secrets/gateway_key
