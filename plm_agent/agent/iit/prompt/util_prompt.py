get_queries_prompt = '''
你是一位医药领域关键信息处理专家,你现在将处理一份医药领域的IIT文档。在该文档中，有三个查询工具需要提取不同的查询参数。你的任务是从文档中识别并提取每个工具所需的查询参数。每个工具的查询需求如下：

1. **pubmed_query**： 提取方案摘要中体现该方案的价值或意义的描述，并形成相关查询如："<某适应症>的<新术式>是否能减少<某不良结果>发生率"。
2. **clinical_guidance_query**：提取IIT文档中的主题或疾病名称，特别是疾病分类、治疗方法等。
3. **policy_query**：提取文档在什么情况下用什么药物对什么疾病展开试验的完整信息
4. **indication_query**：提取文档中提到的基础适应症名称。

以下为iit方案全文；
iit_text
请按照下面的 Json 格式输出每个查询工具的参数。确保每个查询项都简洁且准确：
{
"pubmed_query":"方案的价值或意义相关查询",
"clinical_guidance_query":"临床指南相关",
"policy_query":"政策相关"
"indication_query":"适应症相关"
}
### 可能的改进：
- 根据文档的结构，可以根据不同的章节或段落指示模型进一步精细化查询。比如：
    - Pubmed的引用可能以参考文献或文献综述的形式出现。
    - 临床指南可能出现在治疗标准或最佳实践部分。
    - 政策法规的查询参数由试验的大概内容决定，这一部分一般出现在文档开头部分。
- 注意事项：仅输出 json ，不附带任何额外输出
'''

get_drug_manuals_query_prompt = """
# Role
你是一名资深的临床试验稽查员（CRA）和药学数据专家。你的任务是从临床试验方案中提取出**受试者实际使用的具体药物**，用于查询药品说明书数据库。

# Input
**Protocol_Text**: 临床试验方案原文。

# Extraction Goal
仅提取方案规定受试者**“正在使用”、“将要使用”或“允许合并使用”**的**具体药品名称**。

# Critical Rules (必须严格执行的判定逻辑)

## 1. 垃圾词熔断 (Generic Class Circuit Breaker) - 【核心痛点修复】
**严禁提取无法对应单一说明书的“药理学大类名称”。**
* **规则**：如果原文仅提及“使用降压药”、“禁用抗生素”、“使用糖皮质激素”，但**未指明具体成分**：
    * **操作**：**直接丢弃，不要提取该类名！**（因为搜不到说明书）。
    * **例外**：只有当类名有极其明确的**单一**代表药（如“NK-1拮抗剂”在止吐方案中通常指“阿瑞匹坦”）时，才允许提取该**代表药**（而非类名）。
* **负向反例 (DO NOT EXTRACT)**：
    * ❌ "降压药", "降糖药", "调脂药", "抗凝药"
    * ❌ "抗生素", "β-内酰胺类", "喹诺酮类"
    * ❌ "免疫抑制剂", "糖皮质激素", "吸入性支气管扩张剂"
    * ❌ "止泻药", "止吐药" (除非文中后面括号里写了“如洛哌丁胺”)

## 2. 语境雷达 (Context Radar)
* **✅ 绿区 (提取)**：
    * **干预措施**：核心研究药物（需提取通用名+商品名）。
    * **救援/对症治疗**：明确指示“若发生X则给予Y”的药物。
    * **允许的合并用药**：白名单中的具体药物。
* **❌ 红区 (禁区 - 严禁提取)**：
    * **排除标准 (Exclusion)**：“4周内使用过...”、“对...过敏者”。
    * **禁止用药 (Prohibited)**：“研究期间禁止使用...”。
    * **背景/既往史**：“既往研究显示...”、“传统治疗如...”。

## 3. 实体标准化 (Entity Standardization)
* **剂型补全**：
    * 若原文写“口服洛哌丁胺”，输出“洛哌丁胺胶囊”（补全最常用口服剂型）。
    * 若原文写“静脉给予昂丹司琼”，输出“昂丹司琼注射液”。
* **益生菌特处理**：
    * 提取商品名（如“微适盾”）、通用名（“复合益生菌”）、以及核心菌株名。

# Output Format
仅输出一个去重后的药物列表。列表内不应包含重复项。

output example:
"具体药品名1", "具体药品名1片剂", "代表药A", "代表药A注射液"...
"""

get_drug_paragraghs_prompt = """
作为一个科研文档用药段落抽取模拟器。

[核心任务]
遍历输入文本，提取所有描述具体的“药物使用”、“给药方案”、“剂量说明”、“合并用药限制”或“不良反应处理”的段落。

[严格约束]
1. 保持原文：提取的内容必须与原文完全一致，不得修改、删减或改写。
2. 段落去重（关键）：如果原文中不同位置出现了**内容完全相同**或**高度相似（语义重复）**的段落（例如摘要和正文中重复出现的给药方案），**仅保留信息最完整的一处**，不要输出重复项。
3. 排除无关：排除仅提及药名但无具体操作的目录、标题或页眉页脚。

[输出格式]
输出一个 JSON 列表，包含提取出的独立段落。
"""

check_policy_prompt='''
你是一位临床政策分析专家和人工智能助理。我将给你两段信息：

1. **查询信息（policy_query）**：描述研究者想要对哪种药物、针对什么适应症、以及怎样的临床试验进行政策相关查询。
2. **政策检索结果（policy_result）**：从政策数据库中检索出来的一个或多个政策。

任务：

* 判断 policy_result 是否对 policy_query 有实际参考价值。
* 如果有用，请返回修改后、只保留对 policy_query 有用的内容的 policy_result；
* 如果完全不相关，请返回空（空字符串即可）。
* 分析时只保留对临床试验设计、药物适应症或政策合规有实际指导意义的内容，去掉无关部分。
* 不需要评分或解释，只返回处理后的 policy_result。

请处理以下内容：

policy_query: {policy_query}
policy_result: {policy_result_content}
'''

drug_judgement_prompt = """
# Role
你是一位资深的临床研究方案（IIT）审核专家。你擅长分析临床试验方案，精准识别研究类型、干预措施及药物的具体制剂形式。

# Task
你的任务是阅读用户提供的【IIT临床研究文档】，完成以下工作：
1. 判断该研究的“主要干预措施”是否属于“药物干预”。
2. 如果是药物干预，提取**精确的药物名称（包含通用名和剂型/给药途径）**。
   - *目标是明确药物的具体形态，以便后续查询对应的药品说明书。*
3. **关键任务**：如果主要干预是药物，请务必从文档中摘录**涉及该药物具体使用方案的原文句子或段落**（如用法、用量、给药途径、疗程等）。

# Logic Rules (至关重要)
1. **识别主要干预**：在文档的“研究设计”、“干预措施”、“分组”或“目的”章节中找到核心研究变量。
2. **区分药物与非药物**：
   - 非药物：手术、器械、诊断试剂、心理量表、康复训练、单纯观察。
   - 药物：中药、生物制剂（抗体、细胞治疗等）、化学药物、疫苗。
3. **区分干预药与背景药**：
   - **提取对象**：作为研究变量的试验药物（Investigational Product）。
   - **排除对象**：基础治疗药物、背景疗法、合并用药（除非研究目的是比较不同的基础治疗方案）。

4. **药物名称精准度原则 (Precision of Drug Name)**：
   - **拒绝仅提取通用名**：不能只输出“紫杉醇”或“阿司匹林”。
   - **必须包含剂型或给药方式**：必须根据文中描述，提取为“紫杉醇脂质体注射液”、“阿司匹林肠溶片”、“布地奈德混悬液”（雾化用）或“布地奈德鼻喷剂”。
   - **处理逻辑**：
     - 如果文中明确写了剂型（如“片”、“胶囊”、“注射液”、“软膏”），必须包含在名称中。
     - 如果文中未写明剂型，但写了给药途径（如“口服”、“静脉滴注”），请组合成描述性名称（如“某某药(口服)”、“某某药(注射)”）。

5. **证据提取原则 (Evidence Extraction)**：
   - **必须摘录原文**：不要自己总结，保持原汁原味。
   - **内容聚焦**：摘录的内容应包含干预药物的名称、剂量、频率、**给药途径（Route）**或分组方案。这是判断剂型的关键依据。

# Output Instruction
请按照提供的JSON Schema输出，不要包含任何Markdown格式标记。
- `target_drugs`: 列表中的字符串应为“通用名+剂型”的格式。
- `drug_usage_evidence`: 必须是原文摘录。
- 如果 `is_drug_intervention` 为 `False`，其他字段返回空列表 `[]`。

# Extraction Steps
1. 阅读文档，定位主要研究目的。
2. 判断主要干预是否为药物。
3. **如果是药物，在文中寻找描述该药物形态的关键词（如：片、粒、注射液、粉针、吸入、涂抹等）。**
4. 组合“通用名”+“剂型/给药修饰词”，存入 `target_drugs`。
5. 搜索并摘录该药物具体给药方案的原文段落到 `drug_usage_evidence`。
6. 生成最终JSON。

# Document Content
{iit_text}
"""

is_rct_prompt = """
你是一位临床研究设计专家。请阅读给定的 IIT（研究者发起的试验）方案文本，并严格根据下方的【判定标准】判断该研究是否为随机对照试验（RCT）。

### 判定标准 (必须严格执行)

**（1） RCT 的强阳性信号（出现 1–2 个基本就可判定为 RCT）**
* **1. 明确“随机分配/随机化”**
    * **关键词**：随机、随机分配、随机序列、分层随机、区组随机、中心随机、随机号、随机表、随机系统（IWRS/IVRS/EDC随机模块）
    * **描述要点**：随机序列生成方法、分配隐藏、实施者/权限、分层因素、区组大小（可不写，但写了更强）
* **2. 明确 对照组 且为并行比较设计**
    * **关键词**：对照组、试验组/对照组、两组/多组、平行对照、安慰剂对照、标准治疗对照、阳性对照
    * **结构线索**：研究目的/主要终点是“比较 A vs B”或者“比较 A vs B vs C vs ……”
* **3. 统计假设是“组间比较”的框架**
    * **关键词**：优效/非劣/等效、双侧/单侧检验、delta（非劣界值）、组间差异、HR/OR/均数差、分层检验、ITT/PP 比较
    * **样本量**：按“两组比较/组间差异”计算（而不是单臂阈值）
    * *注：满足以上任一条 + 存在干预 基本就可以判为 RCT。*

**（2） 非 RCT 的强阴性信号（出现 1 个就应强烈怀疑“不是 RCT”，或至少不是“经典 RCT”）**
* **关键词**：单臂、单组、无对照、探索性单臂、真实世界单臂
* **1. 剂量探索/剂量递增设计（典型不是 RCT 主线）**
    * **关键词**：剂量递增、dose-escalation、DLT、MTD、RP2D、3+3、BOIN、CRM、mTPI、扩展队列（expansion cohort）
    * **结构线索**：主要目的为安全性/耐受性/推荐剂量，而非组间疗效比较
* **3. Simon 两阶段/单臂 II 期阈值设计**
    * **关键词**：Simon two-stage、两阶段设计、无效阈值/有效阈值（p0/p1）、拒绝/接受规则、早停规则（单臂）
* **4. 历史对照/外部对照为核心比较依据**
    * **关键词**：历史对照、外部对照、real-world external control、对照来自数据库/既往队列
* **5. 观察性/队列/病例对照/横断面**
    * **关键词**：观察性、回顾性、前瞻性队列、病例对照、横断面、暴露-结局关联、风险因素分析
    * **方法学词**：倾向评分（PSM/IPTW）、多变量回归控制混杂（这些更像“非随机比较”）

### 任务要求
1.  **分析 Reasoning**：在 `reasoning` 字段中，请简要说明判定过程。
    * 指出文中是否出现了【强阳性信号】中的具体关键词（如“随机表”、“IWRS”等）。
    * 指出文中是否出现了【强阴性信号】中的具体关键词（如“3+3”、“单臂”等）。
    * 如果同时出现（例如先剂量递增后随机），请以是否存在“随机对照阶段”为准。
2.  **给出结论**：在 `is_rct` 字段中输出布尔值。

### 待分析的 IIT 方案全文

{iit_text}
"""

expand_drug_prompt = """
你是一个专业的临床医学与药学知识库助手。
任务：为提供的药品名称生成精准的别名（包括：标准通用名、特定商品名、习惯简称/英文缩写）。

【处理规则与输出要求】
1. 关注剂型与给药途径：输入名称中通常包含剂型（如“胶囊”、“混悬液”）或给药途径（如“（注射）”）。这些信息对确定特定的商品名极其重要。请务必结合这些信息，生成与该特定剂型或给药方式严格对应的商品名和别名。
2. 纯净格式：只输出别名，多个别名之间用英文逗号 (,) 分隔。
3. 包含原始输入的药品名：生成的别名列表中必须包含输入的药品名称（如果它本身是一个有效的别名）。
3. 不要包含“输出：”等任何解释性文字或 Markdown 标记。
4. 如果没有确定的别名，请直接返回原始输入的药品名。

【参考示例】
输入：布洛芬缓释胶囊
布洛芬缓释胶囊,芬必得,Ibuprofen Sustained-Release Capsules

输入：布洛芬混悬液
入：布洛芬混悬,美林,Ibuprofen Suspension

输入：阿霉素(注射)
阿霉素(注射),注射用盐酸多柔比星,亚德里亚霉素,羟基柔红霉素,Adriamycin,Doxorubicin Injection,ADM,DOX

【当前任务】
输入：{missing_drug}
"""

translate_iit_prompt = """
【角色设定】
你是一位拥有 15 年经验的资深医学翻译专家，精通 ICH-GCP 准则及临床试验规范。你的翻译风格极其严谨，视“绝对忠实原文”为最高职业准则。

【任务目标】
请将我提供的【IIT审查结果】从中文翻译成英文。

【核心翻译原则与红线约束】

最高红线：绝对忠实原文 (Absolute Fidelity)： >    - 你的翻译必须严丝合缝地贴合中文原文的字面意思和逻辑结构。

禁止任何形式的自行发挥、意译、内容总结或信息省略。

禁止添加任何原文中没有的背景信息或解释。即使原文表述显得冗长或生硬，也必须完整、毫无遗漏地翻译出所有细节和条件状语。

术语精准 (Terminology Accuracy)： 严格使用临床试验和伦理审查领域的国际标准术语（如 IRB/EC, ICF, PI, Protocol, Benefit-Risk Assessment 等）。

格式与结构 1:1 镜像 (Mirror Formatting)： 严格保留原文的所有段落、换行、列表符号、编号逻辑和标点符号的位置。原文分几句话，译文就必须分几句话；原文的表格，译文必须保持同等结构的表格。

客观与中立 (Objective Tone)： 保持高度正式、客观的官方公文语调。不带任何感情色彩，只做原文的“镜子”。

疑难与错误处理 (Handling Ambiguity)： >    - 如果原文存在病句、逻辑矛盾或拼写错误，不要尝试去修正它，请按照字面意思直接翻译。

若遇到无法确定的专有名词（如特定的人名、极其冷门的机构名），请使用汉语拼音，切勿胡乱猜测（杜绝幻觉）。

【输入区域】
以下是需要翻译的中文 IIT 审查结果：
{iit_review_result}

请直接输出纯英文翻译结果，不要提供任何分析、解释或多余的对话。
"""