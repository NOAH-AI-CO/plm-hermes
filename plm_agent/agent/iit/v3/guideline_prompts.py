
toc_reader_prompt = """目前需要根据以下<IIT方案>，与现有临床指南对照，判断<IIT方案>是否为研究空缺、创新点。我们需要先从<临床指南目录>中找到需要阅读的临床指南章节。请输出需要阅读的页码范围。

<IIT方案>
{iit_text}
</IIT方案>

<临床指南目录>
{guideline_toc}
</临床指南目录>

只输出可能需要阅读的章节。如果没有可能需要阅读的章节，输出空列表[]。"""

# 按用户查询选章节（用于 mindsearch 临床指南 tool，不依赖 IIT）
toc_reader_by_query_prompt = """根据用户查询，从<临床指南目录>中找出与查询最相关的章节。请输出需要阅读的页码范围。

<用户查询>
{query}
</用户查询>

<临床指南目录>
{guideline_toc}
</临床指南目录>

只输出可能需要阅读的章节。如果没有可能需要阅读的章节，输出空列表[]。"""

# 输出形式为列表，格式为：
# [{{"section": "章节标题", "page_range": "起始页-结束页"}}, ...]

# 批量选章节：根据用户查询，对多份指南分别从各自目录中选出与查询最相关的章节（用于 mindsearch 临床指南 pipeline，一次 LLM 替代多次）
toc_reader_by_query_batch_prompt = """根据用户查询，对以下每份临床指南，从该指南的<目录>中分别找出与查询最相关的章节。对每份指南输出需要阅读的页码范围。

<用户查询>
{query}
</用户查询>

<多份临床指南>（每份包含 id、标题、目录，请按 id 区分）
{guidelines_block}
</多份临床指南>

对每份指南只输出该指南下可能需要阅读的章节；若某份指南没有相关章节，该指南的 sections 输出空列表 []。
输出格式为 JSON：{{ "results": [ {{ "guideline_id": "指南id", "sections": [ {{ "section": "章节标题", "page_range": "起始页-结束页" }} ] }}, ... ] }}，results 顺序与上面指南顺序一致。"""
