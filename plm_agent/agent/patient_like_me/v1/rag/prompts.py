"""
PLM-333 Dify workflow prompt templates.
Extracted from PLM-333-c2.yml, each prompt corresponds to a named node in the Dify DAG.
"""

# ── Node: 问题改写 (gemini-3.1-pro) ──
EXTRACT_PATIENT_INFO_SYSTEM = "你是严谨的病例信息提取助手，只基于用户原始输入提取结构化信息。"

EXTRACT_PATIENT_INFO_USER = """请从用户输入 {input} 中提取结构化字段。

要求：
1. 只基于用户原文，不添加任何外部信息；无法确定时对应字符串字段留空。
2. `test_results` 必须完整保留全部阳性与阴性检查结果，包括免疫组化、分子基因、病理、影像和生化信息，不得概括或遗漏。
3. `current_symptoms` 仅提取用户原文中已有的当前症状与主诉。
4. `has_clear_diagnosis` 表示根据原文信息能否确定疾病诊断。原文直接写明诊断、或检查结果已足以确定诊断（如特征性融合基因阳性、病理确认等），均视为 true。
5. `primary_diagnosis` 提取疾病诊断名称。原文直接写明的直接提取；原文未写明但检查结果可明确推断的，写出推断的诊断名称。无法确定则留空。
6. `current_medications` 提取患者当前正在使用的所有药物，用逗号分隔。包括处方药、中药等。如原文未提及用药则留空。
7. 仅返回与 schema 一致的结果。"""

EXTRACT_PATIENT_INFO_SCHEMA = {
    "type": "object",
    "properties": {
        "age": {"type": "string"},
        "gender": {"type": "string"},
        "current_symptoms": {"type": "string"},
        "test_results": {"type": "string"},
        "has_clear_diagnosis": {"type": "boolean"},
        "primary_diagnosis": {"type": "string"},
        "current_medications": {"type": "string"},
    },
    "required": ["has_clear_diagnosis", "current_symptoms", "test_results", "gender", "age", "primary_diagnosis"],
}


# ── Node: 是否诊断明确 ──
DIAGNOSIS_CLARITY_CHECK_SYSTEM = "你是严谨的临床指南判读助手，只基于提供的指南内容判断当前诊断明确度。"

DIAGNOSIS_CLARITY_CHECK_USER = """请仅基于指南内容，为患者判断当前诊断明确度。

# 指南内容
{guide_content}

# 患者信息
{patient_info}

判读口径：
1. "是否大致明确"仅表示当前证据下是否能给出基本明确的诊断或分型，不等于后续不需要补充检查。
2. 若诊断或分型仍不够明确，列出仍欠缺的关键检查。
3. 每条结论尽量附对应原文与定位；若提供内容中找不到直接依据，明确写"证据不足（未在提供内容中找到直接依据）"，不要虚构引用。

输出包含：
1. 是否大致明确诊断
2. 当前最可能的诊断/分型
3. 仍需补充的检查及原因"""


# ── Node: 结果结构化 (gemini-flash) ──
STRUCTURED_DIAGNOSIS_SYSTEM = "你是结构化结果整理助手，只将已有判断压缩为 schema 对应字段。"

STRUCTURED_DIAGNOSIS_USER = """请根据结果 {diagnosis_text} 输出与 schema 一致的结构化结果。

规则：
1. `isDiagnosisClear` 仅表示当前证据下是否已达到"基本明确诊断/分型"；即使仍建议补充检查，只要当前诊断已基本明确，也可以为 true。
2. `primaryDiagnosis` 提取当前最可能的疾病诊断名称（如"急性早幼粒细胞白血病"），无法确定则留空。
3. `diagnosisBasisAndRecommendations` 汇总诊断依据，以及仍需补充的关键检查建议。
4. 仅返回 schema 对应内容，不要输出额外说明。"""

STRUCTURED_DIAGNOSIS_SCHEMA = {
    "type": "object",
    "properties": {
        "isDiagnosisClear": {"type": "boolean"},
        "primaryDiagnosis": {"type": "string"},
        "diagnosisBasisAndRecommendations": {"type": "string"},
    },
    "required": ["isDiagnosisClear", "primaryDiagnosis", "diagnosisBasisAndRecommendations"],
}


# ── Node: 诊断总结(简短) — 仅在诊断不明确时输出 ──
SHORT_DIAGNOSIS_SUMMARY_SYSTEM = "你是严谨的临床总结助手，只基于输入内容重组结论，不添加外部知识。"

SHORT_DIAGNOSIS_SUMMARY_USER = """请基于 {diagnosis_text} 给出：
1. 进一步检查建议
2. 当前最可能的初步诊断
3. 需要考虑的鉴别诊断

要求：
1. 只基于输入内容重组，不新增未被支持的结论。
2. 引用只能来自已提供内容；有直接依据时给出引用，没有直接依据时明确写"证据不足"。
3. 用简洁、专业的临床报告风格输出。"""


# ── Node: 诊断总结  — 诊断指南搜索后 ──
DIAGNOSIS_REPORT_SYSTEM = "你是严谨的临床指南提取助手，只基于提供的指南内容生成报告。"

DIAGNOSIS_REPORT_USER = """请仅基于指南内容，为患者总结诊断及依据，并生成带引用的规范报告。

# 指南决策图谱(最高优先级证据,与下方"指南内容"冲突时一律以本段为准;仅用于判断决策路径,不是可引用来源,不得为其编号,也不得写入"参考文献")
{graph_evidence}

# 指南内容(辅助证据)
{guide_content}

# 患者信息与问题
{patient_info}

# 核心约束
1. **本段只输出"诊断结论与依据"，进一步检查、治疗方案分别由独立板块生成，本段不要涉及检查项目清单或治疗/用药方案。**
2. **图谱是从原指南流程图中抽取的结构化决策路径,反映指南最权威的判断分支;如本段与下方"指南内容"片段不一致,以图谱为准。**
3. 诊断结论或排除判断如有量化阈值，必须写出指南中的具体阈值。
4. 如指南直接提及与本患者相关的极高危预后因素、耐药警示或分型预警，必须提取；未提及则写"无"。
5. 诊断标准如涉及多条件组合路径，按原文阶梯/分支结构展开，不要简化为计数规则。

# Citation & Reference Rules
1. **本段独立编号**：引用编号从 [1] 开始重新计数；正文引用用纯数字方括号, 多个相邻并列写成 `[1][2]`（禁止 `[1, 2]` 这种逗号合并; 单处不超过 3 个），不要使用 `E数字` / `citation:` 等任何其他形式。
2. **文末参考文献**：在本段最末以"参考文献："起一行，下方按 `[1]`、`[2]` 顺序列出本段正文实际用到的引用。每行格式严格遵循 `[N] （版本号）指南名 | 章节名 | pages页码范围`，直接照搬上方每条证据头部的标签，不得改写或省略。
3. **严禁幻觉**：仅限使用提供的内容，严禁添加任何外部知识或推论。

# Output Template (严格按此格式输出)
## 诊断建议及依据
[诊断结论与依据，引用使用 [1] / [1][2] 形式]
指南附加安全与执行说明
* 极高危预后与分型预警：[若指南未提及则输出"无"]
参考文献：
[1] （版本号）指南名 | 章节名 | pages范围
[2] ..."""


# ── Node: 进一步检查总结 (gemini-flash) ──
EXAMINATION_REPORT_SYSTEM = "你是严谨的临床指南提取助手，只基于提供的指南内容生成报告。"

EXAMINATION_REPORT_USER = """请仅基于指南内容，为患者总结进一步检查及依据，并生成带引用的规范报告。

# 指南决策图谱(最高优先级证据,与下方"指南内容"冲突时一律以本段为准;仅用于判断决策路径,不是可引用来源,不得为其编号,也不得写入"参考文献")
{graph_evidence}

# 指南内容(辅助证据)
{guide_content}

# 患者信息与问题
{patient_info}

#核心约束：
1. **图谱是从原指南流程图中抽取的结构化决策路径,反映指南最权威的判断分支;如本段与下方"指南内容"片段不一致,以图谱为准。**
2. 识别指南中带有年龄/性别等前置条件的检查项，结合患者信息写明保留或排除理由。
3. 在"进一步检查"正文中，对该患者适用的检查项目要写明；对仅在特定条件下适用的项目（替代检查、可选检查、补充检查），不要直接删除，应以"可选""必要时""若……则……"的形式简要保留并写明适用条件，只有与本病例明显无关或有明确不适用条件的项目才排除。
4. 除常规检查外，可根据患者已知高危标签补充专科特异性评估，但必须说明对应的患者特征依据；如无直接依据，不要硬加。
5. 必须提取指南中与分型完善、分期评估、基线器官功能和致命并发症防范相关的必查项目及其因果关系；同时必须保留每项检查在结果不明确、样本不合格或异常时的下一步处理（如重测、补充取材、追加影像/活检、改用替代检查），这类内容是临床决策最依赖的部分，不得省略。

# Citation & Reference Rules
1. **本段独立编号**：引用编号从 [1] 开始重新计数；正文引用用纯数字方括号, 多个相邻并列写成 `[1][2]`（禁止 `[1, 2]` 这种逗号合并; 单处不超过 3 个），不要使用 `E数字` / `citation:` 等任何其他形式。
2. **文末参考文献**：在本段最末以"参考文献："起一行，下方按 `[1]`、`[2]` 顺序列出本段正文实际用到的引用。每行格式严格遵循 `[N] （版本号）指南名 | 章节名 | pages页码范围`，直接照搬上方每条证据头部的标签，不得改写或省略。
3. **严禁幻觉**：仅限使用提供的内容，严禁添加任何外部知识或推论。

# Output Template (严格按此格式输出)
## 进一步检查
[此处填入进一步检查与依据，引用使用 [1] / [1][2] 形式]

指南附加安全与执行说明
* 核心器官与并发症筛查红线：[若指南未提及则输出"无"]
参考文献：
[1] （版本号）指南名 | 章节名 | pages范围
[2] ..."""


# ── Node: 治疗总结 (gemini-flash) ──
TREATMENT_REPORT_SYSTEM = "你是严谨的临床指南提取助手，只基于提供的指南内容生成报告。"

TREATMENT_REPORT_USER = """请仅基于指南内容，为患者总结治疗及依据，并生成带引用的规范报告。

# 指南决策图谱(最高优先级证据,与下方"指南内容"冲突时一律以本段为准;仅用于判断决策路径,不是可引用来源,不得为其编号,也不得写入"参考文献")
{graph_evidence}

# 指南内容(辅助证据)
{guide_content}

# 患者信息与问题
{patient_info}

# 核心约束：
1. **图谱是从原指南流程图中抽取的结构化决策路径,反映指南最权威的判断分支;如本段与下方"指南内容"片段不一致,以图谱为准。**
2. 识别患者核心高危特征，例如（高龄、ECOG、器官风险、分子遗传学高危等），并据此筛选治疗方案。
3. 推荐方案需与患者特征比对，原文中明确的禁忌或路径限制方案不要作为推荐列出；不需要在输出中展示排除推理过程。
4. 若原文明确给出剂量调整方案，作为推荐内容；原文未支持的剂量不要自行发明。
5. 涉及复发难治或多条件分支时，可使用 Markdown 表格增强清晰度。
6. **覆盖指南推荐的所有治疗模式，不要因为讲药物方案而省略本地治疗**：仔细扫描指南内容，凡指南为本例推荐的治疗模式（如局部治疗 / 系统治疗 / 支持治疗等）都要呈现；不适用的不要凭空补。具体术式 / 剂量 / 药物按指南原文表述，不要套用其他病种的模板词。
7. 保留治疗的分层结构（核心方案、可选方案、替代方案、条件性方案），最符合本例的核心方案排首位；不要把多个具体治疗方式合并为笼统上位词。

# Citation & Reference Rules
1. **本段独立编号**：引用编号从 [1] 开始重新计数；正文引用用纯数字方括号, 多个相邻并列写成 `[1][2]`（禁止 `[1, 2]` 这种逗号合并; 单处不超过 3 个），不要使用 `E数字` / `citation:` 等任何其他形式。
2. **文末参考文献**：在本段最末以"参考文献："起一行，下方按 `[1]`、`[2]` 顺序列出本段正文实际用到的引用。每行格式严格遵循 `[N] （版本号）指南名 | 章节名 | pages页码范围`，直接照搬上方每条证据头部的标签，不得改写或省略。
3. **严禁幻觉**：仅限使用提供的内容，严禁添加任何外部知识或推论。

# Output Template (严格按此格式输出) ：
## 治疗建议及依据
**治疗目标与总体策略**
* **总体原则**：[引用使用 [1] / [1][2] 形式]
* **高危与合并症管理**：[引用使用 [1] / [1][2] 形式]

**治疗模式总览**
扫描指南内容，按指南或图谱顺序，把对本例适用的所有治疗模式逐项列出。每项独立一行，遵循以下要求：
* **不要只用一句话带过**：指南为本例写明的具体操作、药物 / 剂量 / 给药方式 / 周期 / 关键技术要点等都按原文措辞写到对应模式下；指南没写细节就用原文表述概括，不要凭空补充。
* **不要因为该方案在指南里被标注为"次选 / 可考虑 / 特定情况适用"或带有任何条件分级而省略**：只要指南对本例给出过推荐（无论强弱），都要在"可选 / 备选方案"里列出，并保留指南原文标注的强度/条件描述（如"可考虑用于...""适用于...""作为备选"等）。

**一线方案细节**
* **标准方案（适合体能良好 / 无禁忌者）**：[展开上方"治疗模式总览"里的核心方案 — 含指南原文写明的所有具体操作 / 药物 / 剂量 / 周期等细节]
* **可选 / 备选方案**：[指南为本例写明"次选 / 可考虑 / 特殊人群适用 / 替代方案 / 各种条件分级的备选项"都在这里逐条列出，并照搬指南原文措辞与条件描述。指南没写就省略]
* **特定风险调整方案（结合本例）**：[针对患者高危点的调整]

**方案选择要点（结合本例）**
* [结合本例的具体高危点给出应对建议]

**指南附加安全与执行说明**
* **剂量与毒性防线**：[若指南未提及则输出"无"]
* **监测与协作指导**：[若指南未提及则输出"无"]
参考文献：
[1] （版本号）指南名 | 章节名 | pages范围
[2] ..."""





# ── Node v1: 三个收尾块(核心摘要 + 临床关键风险 + 医患沟通) ──
# beta-v1 改造:诊断/检查/治疗 三块由代码层直接拼接,不再走 LLM。
# 本 prompt 只产出报告"头部"病例核心摘要 + "尾部"临床关键风险 / 医患沟通。
REPORT_SUMMARY_RISK_COMM_SYSTEM = (
    "你是严格的医学内容整合助手，只基于输入事实重组文本，不新增结论。"
)

REPORT_SUMMARY_RISK_COMM_USER = """以下是患者三个独立的指南板块输出。请基于这些内容产出三个收尾块(病例核心摘要 / 临床关键风险与执行要点 / 医患沟通核心要点)，不要重写或重述诊断/检查/治疗本身。

【诊断总结】
{diagnosis_summary}

【进一步检查总结】
{examination_summary}

【治疗总结】
{treatment_summary}

# 核心约束
1. **【病例核心摘要】**
   - 一个自然段表达，不要表格。
   - 250-450 字左右。
   - 必须基于上方三段提炼，不得新增未被上游支持的医学结论。
   - 概括本病例最中心的诊疗判断和下一步处理方向。内容可涉及：核心诊断或疑似诊断、当前关键疾病状态/分期/风险分层、最需要优先处理的临床问题、下一步关键检查或评估方向、主要治疗策略、核心用药。
   - 不要堆叠所有检查或治疗细节(那些已在前文报告中)。
   - 不要加入参考文献编号、指南名称、指南年份、章节名或文件名。
   - 以"详细建议如下。"结尾。

2. **【临床关键风险与执行要点】**
   - 只基于三段输入的"指南附加安全与执行说明"提炼。
   - 根据病例和指南事实动态保留最相关的风险/执行模块,不限定固定数量。可保留的模块包括但不限于：极高危预后预警、中枢神经系统风险、肿瘤溶解综合征(TLS)预防、乙肝再激活风险、骨髓检查规范、器官毒性防线与用药精调、全病程管理与跨科室协作。
   - 不为凑格式硬写无关模块。

3. **【医患沟通核心要点】**
   - 写成面向医生的"需要明确告知患者/家属的要点清单"。
   - 必须结合病例和指南事实生成,尽量覆盖以下维度并按病例取舍:疾病风险本质说明、治疗路径与策略选择、预后与治疗目标管理、治疗相关风险与并发症说明、生活质量与长期影响、患者参与决策。
   - 若本例涉及高危生物学特征、治疗分歧、重大毒性风险或长期影响,必须展开对应沟通点;不得写成空泛套话。
   - 每条要点尽量写成"医生需要明确告知患者什么、为什么、与本例有什么关系"。

4. **引用**:
   - 【病例核心摘要】不要带任何引用编号。
   - 【临床关键风险与执行要点】可以引用上游三段已出现的具体编号，使用 `[1]` / `[1][2]` 这种纯数字方括号格式(多个相邻并列, 不要逗号合并)；该块在最终报告中独立编号，本 prompt 视上游已用过的编号作"本块新编号"，从 [1] 开始。
   - 【医患沟通核心要点】不需要引用。
   - 整个 prompt 输出禁止出现 `[citation:`、`E数字`、`citation:E*` 等任何形式的内部证据 ID。
5. 不输出开场白、结束语或额外说明。

# Output Template(严格按此格式输出)
## 病例核心摘要
[一个自然段,250-450 字,以"详细建议如下。"结尾]

## 二、临床关键风险与执行要点
[动态模块,按病例取舍，引用使用 [1] / [1][2] 形式]
参考文献：
[1] （版本号）指南名 | 章节名 | pages页码范围
（按本段正文实际引用的顺序列出，编号从 [1] 开始；若本块无引用则省略整个"参考文献："段）

## 三、医患沟通核心要点
[要点清单，不需要引用]"""


# ── Node: 指南总结 (gemini-flash) — 整合诊断/检查/治疗三个总结 ──
# (保留向后兼容: beta-v0 仍然用这个 prompt)
GUIDELINE_INTEGRATION_SYSTEM = "你是严格的医学内容整合助手，只基于输入事实重组文本，不新增结论。"

GUIDELINE_INTEGRATION_USER = """请将患者的诊断总结 {diagnosis_summary}、进一步检查总结 {examination_summary}、治疗总结 {treatment_summary} 整合成最终的指南部分。

#核心约束：
0. 在所有块之前，先输出 `## 病例核心摘要` 块，要求如下：
   1. 放在全文最前面，位于"诊断建议""进一步检查""治疗方案"等详细模块之前。
   2. 用一个自然段表达，不要写成表格。
   3. 应控制在250-450字左右。
   4. 摘要必须基于下方【诊断总结】【进一步检查总结】【治疗总结】中的内容提炼，不得新增未被上游证据支持的医学结论。
   5. 摘要应结合病例背景，概括本病例最中心、关键的诊疗判断和下一步处理方向。内容可涉及：核心诊断或疑似诊断、当前关键疾病状态/分期/风险分层、最需要优先处理的临床问题、下一步关键检查或评估方向、该病例主要治疗策略、针对病症的核心用药等。
   6. 摘要不要堆叠所有检查细节、治疗细节和风险管理细节，详细内容留到后文模块。
   7. 摘要中不要加入参考文献编号。
   8. 摘要中不要出现指南名称、指南年份、章节名或文件名。
   9. 摘要以"详细建议如下。"结尾。
1. 只基于输入事实重组，不新增结论；可合并重复表述，但不得删除上游已出现的检查项目、治疗选项、替代方案、可选项、条件性推荐及其适用条件，也不得把多个具体方案过度概括为笼统上位词。
2. 最终在「病例核心摘要」之后，分成 4 个独立块，且每个块后都跟自己的"参考文献"。
3. `1. 诊断建议` 只使用诊断总结中的"诊断建议及依据"内容。
4. `2. 进一步检查` 只使用进一步检查总结中的"进一步检查"内容。
5. `3. 治疗方案` 只使用治疗总结中的"治疗建议及依据"内容。
6. `二、临床关键风险与执行要点` 只基于三个输入中的"指南附加安全与执行说明"提炼，并在该块后单独列出自己的参考文献。
7. 该块根据病例和指南事实动态保留最相关的风险/执行模块，不限制固定数量，但必须只保留与本病例直接相关的内容。可保留的模块包括但不限于：极高危预后预警、中枢神经系统风险、肿瘤溶解综合征（TLS）预防、乙肝再激活风险、骨髓检查规范、器官毒性防线与用药精调、全病程管理与跨科室协作、医患沟通核心要点。
8. 该块不必每次都出现全部模块，不为凑格式硬写无关模块；但 `医患沟通核心要点` 必须保留，且必须作为该块最后一个子模块输出。
9. `医患沟通核心要点` 不能只写一句总结，必须写成面向医生的"需要明确告知患者/家属的要点清单"。内容必须结合病例和指南事实生成，尽量覆盖以下维度，并按病例取舍，不可脱离事实扩写：疾病风险本质说明、治疗路径与策略选择、预后与治疗目标管理、治疗相关风险与并发症说明、生活质量与长期影响、患者参与决策。若本例明显涉及高危生物学特征、治疗分歧、重大毒性风险或长期影响，必须展开对应沟通点；不得写成空泛套话。每条沟通要点尽量写成"医生需要明确告知患者什么、为什么、与本例有什么关系"。
10. 每个块的正文引用与参考文献列表都只在本块内部编号，从 [1] 重新开始；不要合并成总参考文献区。
11. 每个块的参考文献只列该块正文实际使用到的条目，使用上游传入的完整标签格式。
12. 不输出开场白、结束语或额外说明。

# Output Template (严格按此格式输出) ：
## 病例核心摘要
[一个自然段，250-450 字。最后一句固定为"详细建议如下。"]

1. 诊断建议
[正文内容]
参考文献：
[仅列出本块实际使用的条目，保持 （版本号）指南名 | 章节名 | pages页码范围 的完整格式，编号从 [1] 开始]

2. 进一步检查
[正文内容]
参考文献：
[仅列出本块实际使用的条目，保持 （版本号）指南名 | 章节名 | pages页码范围 的完整格式，编号从 [1] 开始]

3. 治疗方案
[正文内容]
参考文献：
[仅列出本块实际使用的条目，保持 （版本号）指南名 | 章节名 | pages页码范围 的完整格式，编号从 [1] 开始]

## 二、临床关键风险与执行要点
[正文内容]
参考文献：
[仅列出本块实际使用的条目，保持 （版本号）指南名 | 章节名 | pages页码范围 的完整格式，编号从 [1] 开始]"""


# ── Node: 文献精炼器 (gemini-flash, iteration内) ──
ARTICLE_REFINER_SYSTEM = "你是严谨的循证证据筛选助手，负责审核单篇 PubMed 文献的相关性并提取可用治疗证据。"

ARTICLE_REFINER_USER = """阅读文献，提取与患者 `{patient_info}` 相关的治疗证据。

相关性与时间准入：
1. 发表年份距今超过 10 年，直接输出 `SKIP`。
2. 必须严格匹配患者的疾病阶段、病种核心定语和受累部位；不完全匹配则 `SKIP`。
3. 优先保留与患者特殊生理特征、高龄、器官风险或高危亚型直接相关的研究；明显脱节则 `SKIP`。
4. 过滤仅重复金标准常识、明显过时或已被现行指南推翻的证据。

文献信息：
- 标题: {title}
- 摘要内容: {summary}
- 期刊: {journal}
- 年份: {year}
- 卷(期): {volume}({issue})
- 页码: {pagination}
- DOI: {doi}

# 提取要求（文中如有，则应包含以下细节，没有可为空）
1. **样本量与人群**：该研究纳入了多少例患者？具体的亚型（如 GCB vs. non-GCB、EGFR 突变 vs. 野生型、HER2 阳性 vs. 阴性等）分布如何？
2. **具体有效性数据**：不要只说"有效"，写出具体的有效率（ORR）、3年/5年生存率（OS/PFS）或 P 值等。
3. **安全性细节**：提取与患者风险因素相关的毒性数据，如免疫相关不良事件的发生率与分级、HBV再激活时间点、心脏毒性的EF下降阈值、神经毒性的剂量调整规则等。
4. **原始结论原文**：摘录 1-2 句研究中关于临床建议的原始核心论述。

# Output Format
如果是无关文献，直接输出 "SKIP"。
如果是相关文献，请按照以下格式输出：

【临时编号】：[REF_{index}]
【证据原文详述】：[此处填入上述 1-4 点的详细学术陈述，篇幅需在 150-300 字之间]
【参考文献信息】：{first_author}, et al. ({year}). {title}. {journal}."""


# ── Node: 汇总节点 (deepseek-chat) ──
PUBMED_AGGREGATION_SYSTEM = "你是严谨的医学综述编辑，负责将多篇 PubMed 证据整理成结构清晰的循证综述。"

PUBMED_AGGREGATION_USER = """# Input
这是从多篇 PubMed 文献中提取的治疗证据碎片：{evidence_fragments}

 # Task
将上述碎片整合成一篇逻辑严密的诊疗建议综述。

#核心约束：
1. 组织证据时优先使用近 5 年文献；仅在近 5 年证据不足时，才使用 5-10 年内文献补充。
2. 忽略所有 `SKIP` 条目。
3. 不要大篇幅复述已成常识的陈旧金标准结论，重点写新药、新靶点、特殊高危人群或安全性证据。
4. 保留输入中的关键数据，如样本量、百分比、P 值和限定条件，不要过度压缩。
5. 聚焦患者具体高危特征，避免写成泛泛而谈的常规治疗综述。

# 引用重排规则（关键！）
1. **重新排序**：彻底忽略所有 "SKIP" 条目。
2. **按序编号**：根据你在最终报告中引用这些文献的**先后顺序**，重新分配引文编号为 [1], [2], [3]... 直至 [n]。
3. **替换占位符**：将输入中的 [REF_X] 替换为你分配的新编号。
4. **范围合并**：如果一处连续引用了多篇文献，必须合并格式。示例：将 [1, 2, 3] 缩写为 [1-3]；将 [1, 3, 4] 缩写为 [1, 3-4]。

# 输出结构
# 关于 [病名] 的循证诊疗策略分析
## 一、 一线治疗方案的详述
（此处包含 [1-3] 这种格式的引文，且必须根据出现顺序从 [1] 开始）
## 二、 风险评估与安全性证据
（接续上面的编号）
## 三、 前沿证据总结
（必须单起一段，以"综上所述"开头，用2到3句话对上述前沿证据对本病例的具体指导意义进行高度概括，作为正文的收尾。严禁添加其他内容，只进行总结概括。）
## 四、 参考文献列表
（必须严格按照 [1], [2], [3] 的新顺序排列，列出完整的期刊、作者信息）

只输出上述四部分，不要额外开场白。"""


# ── Node: 幻觉审查 (gemini-flash) ──
HALLUCINATION_AUDIT_SYSTEM = "你是最严谨的学术诚信审计员，负责核对文稿中的每一处文献引用是否被事实基准表支持。"

HALLUCINATION_AUDIT_USER = """对比【事实基准表】{truth_table} 与【诊疗建议论文】{draft_text}，检查论文中每一处引用的真实性。

# 审计准则
1. 编号匹配：检查论文中的 [1], [2] 等编号对应的 PMID，其内容是否能在【事实基准表】中找到。
2. 数据审计：如果论文提到"有效率 80%"，而事实表里写的是"70%"，请记录为"数值偏差"。
3. 归属审计：检查结论是否被张冠李戴（例如：把 A 研究的结论标给了 B 研究）。

# Output Format
首先显示【审计结论概览】：
- 引用总数：X 处
- 事实错误：X 处
- 幻觉风险：高/中/低

然后输出【论文全文】：
(此处原封不动显示论文内容)

最后附上【详细审计批注】：
| 编号 | 状态 | 详情 | 原始依据 |
| :--- | :--- | :--- | :--- |
| [1] | ✅ 通过 | 引用准确 | 原文支持此结论 |
| [3] | ❌ 幻觉 | 数值错误：原文 P=0.05，论文写 P<0.01 | 原文：...p value was 0.05... |"""


# ── Node: 提取幻觉部分数据源 (gemini-flash) ──
CLEAN_EVIDENCE_SYSTEM = "你是循证医学数据分析师，负责从审计结果中抽取可继续使用的纯净证据。"

CLEAN_EVIDENCE_USER = """任务：基于审计报告{audit_report}，从原始证据{truth_table}中提取深度事实，生成【纯净证据清单】。
处理规则：
1.保留 ✅ 项：从 truth_table 中提取这些 PMID 对应的原始数据，保留其样本量、P值等核心事实。
2.剔除/修正 ❌ 项：
如果是【归属错误】（如 [6]）：直接删除该条目，禁止出现在后续内容中。
如果是【数值幻觉】（如 [3], [5], [10]）：严禁使用论文中编造的数字。必须回溯 truth_table，如果原文确实没写这些数，则只提取原文中存在的定性描述（例如：只说"评估了预后价值"，不说"108例"）。
3.元数据保留：每一条证据必须附带完整的：作者, 年份, 标题, 期刊, 卷(期), 页码, DOI。
4.输出格式：输出一份"纯净证据清单"，按 [1], [2], [3]... 重新连续编号。
5.严禁事项：禁止输出任何关于"我是谁"、"我改了哪里"的废话。直接输出论文正文。

输出示例
[REF_1] (PMID: 19656156): 纳入730例患者，试验组3年DFS显著优于对照组 (P=0.05)。来源信息：Pavan, A., et al. (2008). XXX. Journal of Clinical Oncology."""


# ── Node: 最终重写 (gemini-flash) ──
FINAL_REWRITE_SYSTEM = "你是严谨的医学期刊编辑，负责基于审计结果对底稿做最小必要修订。"

FINAL_REWRITE_USER = """任务：请基于【审计报告】{audit_report}，对【文稿底稿】{draft_text}进行局部精准手术，生成最终的循证诊疗建议。
修改逻辑（核心指令）：
保留好文笔：最大限度保留【文稿底稿】的叙述逻辑、专业词汇和段落结构。
精准替换幻觉：
凡是【审计报告】中标记为 ✅ 的内容：原样保留。
凡是【审计报告】中标记为 ❌ 的内容（如数值错误、归属错误）：必须彻底删掉底稿中的错误描述，用【纯净证据库】{clean_evidence}中的正确事实进行重写。
示例：如果底稿说"108例患者"，而审计说这是幻觉，原文没提数字，那么请将其改为"相关研究评估了亚型的预后价值"，删掉具体的数字。
引文标注规范：
正文中：统一使用数字编号 [1], [2-3] 格式。
文末参考文献：按照数字顺序排列，但内容必须使用标准的 APA 格式（包含作者、年份、标题、期刊、DOI等）。
输出结构：
[标题]
一、 治疗方案详述
...（正文，带[1]格式引文）...
参考文献
[1] Author, A. A. (Year). Title... (APA格式) [2] Author, B. B. (Year). Title... (APA格式)
严禁输出"四、参考文献"或单独建立文献列表！
只允许输出替换清洗后的正文段落（一、二、三部分），引文使用 [1], [2] 格式嵌入正文中即可。"""


# ── Node: 诊疗总结 (gemini-flash) — 最终合并指南+PubMed ──
FINAL_MERGE_SYSTEM = "你是严谨的临床编辑，负责整合指南内容与 PubMed 证据，并统一引用格式。"

FINAL_MERGE_USER = """# Role
你是一个高精度的医学文献搬运与格式对齐工具，负责将不同来源的内容进行标准化合并，并执行极其严格的引文核对与清洗。

# Task
将【指南来源】{guideline_text} 和 【PubMed来源】{pubmed_text} 的内容合并输出。

# 核心规则 (最高优先级)
1. **引文格式标准化 (核心修正)**：
   - 将指南中的引文按出现顺序统一修改为简洁的数字编号，如 [1], [2]...。

2. **全局引文重排 (Global Re-indexing)**：
- 根据它们在正文中**首次出现**的绝对先后顺序，依次分配全局唯一的递增数字编号：[1], [2], [3]...。

3. **局部文献列表清洗 **：
- 上游传入的文本中，每个大标题末尾自带的"参考文献"列表是全量且混乱的。**在输出时，绝对禁止直接照抄原列表！**
- 强制盘点：你必须像学术审计员一样，盘点当前大标题的正文段落中**实际用到**了哪几个引用编号。
- 在"参考文献"区域，**仅列出正文中实际出现过的文献**，并且必须严格按照数字大小（如 [1], [2], [3]）从小到大升序排列！严禁列出正文中未引用的文献。
- 每条参考文献必须保持 `（版本号）指南名 | 章节名 | pages页码范围` 的完整格式，仅替换编号为数字，不得省略章节名或页码范围。

4. **内容完整性**：必须完整搬运两个输入变量中的所有文本内容，不得进行摘要或过度删减。

5. **严禁事项**：禁止输出任何关于"我是谁"、"我已对齐格式"等废话。直接输出正文。

输出格式：
# 循证诊疗建议汇总

## 一、权威指南共识
{guideline_text}

---

## 三、PubMed 前沿论文证据
{pubmed_final_rewrite}"""


# ── Evidence query generation (LLM-based) ──
EVIDENCE_QUERY_GEN_SYSTEM = "你是临床指南检索助手。根据患者病例生成高质量的检索query，用于从临床实践指南中精准检索相关证据。"

EVIDENCE_QUERY_GEN_USER = """根据以下病例信息，生成5-8条用于检索临床指南的query。

## 要求
1. 每条query要用指南中实际可能出现的术语（药物名、方案缩写、管理流程关键词）
2. 覆盖该病例最关键的信息需求，不要写重复或泛泛的query
3. 如果病例中有高危特征（极高白细胞、器官功能不全、高龄等），要生成针对性的支持治疗/紧急处理query
4. 如果病例涉及特定药物，要生成该药物的安全监测、剂量调整、时间间隔等细节query
5. query要具体到指南中实际会出现的表述方式，而不是泛化的关键词堆叠
6. 如果用户问题里出现了具体的技术/术式/方案/检查项目名称（例如某种放疗类型、术式、内镜操作、生物标志物检测等），至少要有一条query直接包含这些词；不要把它们泛化成"治疗策略""检查方案"这种上位概念
7. 生成的 query 使用英文术语，且必须包含用户问题里的具体术语在英文医学指南中实际使用的英文表达（例如：用户问"气管内梗阻治疗"时，query 要出现 endobronchial obstruction、laser/stent、brachytherapy、external-beam radiation 等指南页上原样使用的英文措辞），以便向量检索能直接命中英文原文 chunk
8. 当用户问"how 该项目应该被执行 / how each test should be performed / 该药物/检测/操作应如何进行"这类问句时，对每个被问到的具体项目（药物、检测、术式、操作等）都要额外生成一条针对其"执行规则 / 异常处理 / 重测条件 / 校准与质控 / principles of X / X testing principles"的 query；指南通常把这些细节集中在 "Principles of ..." 或 "Testing principles" 类的章节，应该用这种章节级措辞作为 query。例如：用户问 HER2 检测时，除了"HER2 testing"本身，还要生成 "principles of biomarker testing HER2"、"HER2 testing retesting suboptimal sample heterogeneity"、"ASCO/CAP HER2 testing guideline" 等 query

## 病例信息
- 诊断: {diagnosis}
- 关键特征: {key_features}
- 检索意图: {intent}
- 用户问题: {user_query}
- 完整病例: {patient_text}

请输出JSON，格式为：
{{"queries": [{{"query": "具体检索词"}}]}}"""


CHUNK_RERANK_SYSTEM = "你是临床指南检索结果筛选助手。你看到的是从向量+词法初步召回的候选指南段落列表，每个段落附带章节标题。你的任务是基于用户的实际问题，挑出对回答这个问题最相关的段落。"

CHUNK_RERANK_USER = """用户问题：
{user_query}

候选指南段落（按初步召回得分排序，每行格式为 编号. [章节标题] 段落摘要）：
{candidates}

请挑出最相关的 N 个段落（N 不超过 {top_k}）。

判定原则：
1. 关注章节标题与用户问题的对齐度。例如用户问治疗策略时，"Recurrence and Metastasis"、"Principles of Treatment" 类章节优先；用户问检查/检测时，"Principles of Biomarker Testing"、"Workup"、"Diagnostic Procedures" 类章节优先。
2. 用户问题里出现的具体术语（药物名、检测名、术式名）应在所选段落里出现或属于该章节范畴。
3. 不要只看摘要文字命中，要看章节定位是否对路；段落摘要只是初步信号。
4. 如果某段落是另一段落的脚注引用（如仅列 "see NSCL-18"），跳过它，选真正含原文的段落。

请只输出 JSON，格式为：{{"selected": [编号1, 编号2, ...]}}（编号是上面候选列表里的数字）"""


EVIDENCE_GAP_DETECT_SYSTEM = "你是临床证据完整性审查助手。判断已检索的证据是否完整覆盖病例的关键信息需求。"

EVIDENCE_GAP_DETECT_USER = """检查以下已检索证据是否有明显缺失。

## 病例信息
- 诊断: {diagnosis}
- 关键特征: {key_features}
- 完整病例: {patient_text}

## 已检索到的证据段落
{evidence_summary}

## 要求
如果有临床上重要但未被覆盖的信息需求（如缺少支持治疗、缺少特定药物的安全细节、缺少评估时间节点等），生成1-3条补充检索query。
如果证据已足够，返回空列表。
不要重复已有证据覆盖的内容。

输出JSON：{{"gap_queries": [{{"query": "...", "evidence_type": "..."}}]}}"""


# ── Tool query templates ──
SEARCH_DIAGNOSIS_QUERY = "{diagnosis} 诊断标准 分型 危险分层 {key_features}"

SEARCH_CSCO_QUERY = "请根据该患者的症状{current_symptoms}和检查结果{test_results}查询相应的CSCO指南检查和诊断部分。"

SEARCH_EXAMINATION_QUERY = "{diagnosis} 检查评估 实验室检查 影像学 {key_features}"

SEARCH_TREATMENT_QUERY = "{diagnosis} 治疗方案 诱导治疗 巩固治疗 {key_features}"

PUBMED_TREATMENT_QUERY = "{diagnosis} {key_features} 治疗"

# ── LLM doc selection prompt ──
SEARCH_DOC_SELECT_PROMPT = """你将从候选文档中选择与查询最相关的最多5个文档。

核心规则：
1. 必须优先选择与患者确诊疾病直接相关的指南文档。
2. 若患者确诊疾病是某个大类下的亚型，覆盖该亚型完整诊疗路径的**综合大类指南**（而非仅针对某单一亚型的专项共识）通常更适合生成报告，应选入并优先排在前面；若同时存在该亚型的专属指南，可一并选入（大类在前）。
3. 不要选择与患者疾病无关的指南，即使它们在症状描述上有相似之处。
4. 如果同一指南有中文版和英文版，只选英文版（不带"zh"后缀的原版），不要同时选两个版本。
5. 只返回JSON对象，格式为 {{"ids": ["id1", "id2"]}}。

患者确诊/疑似疾病：{diagnosis}
查询: {query}

候选文档:
"""

# ── Patient text structuring ──
STRUCTURE_PATIENT_TEXT_SYSTEM = "你是严谨的临床信息整理助手。将患者的所有原始信息（用户输入、附件内容、结构化补充）统一整理为清晰的结构化文本。"

STRUCTURE_PATIENT_TEXT_USER = """请将以下患者原始信息整理为一份结构化的患者病例摘要文本。

## 原始信息
{raw_input}

## 要求
1. 输出纯文本格式（非JSON），使用清晰的分段和标题
2. 按以下维度组织信息（有则写，无则跳过该维度）：
   - 基本信息（年龄、性别等）
   - 主诉与现病史
   - 诊断（已确定的或疑似的）
   - 实验室检查（血常规、生化、免疫组化、分子基因等）
   - 影像学检查
   - 病理检查
   - 既往史/合并症
   - 治疗史（既往用药、手术、放化疗等）
   - 当前体征/症状
   - 当前用药
   - 其他临床相关信息
3. 完整保留原文中的所有数值、检查结果、阳性/阴性指标，不得概括或遗漏
4. 整合重复信息，去除无关内容（如系统提示语、格式标记等）
5. 保持专业医学术语，不做通俗化改写
6. 不添加任何原文中没有的推断或信息"""

# ── Secondary guideline comparison ──
# 把"主报告(诊断+检查+治疗)"与"各次要机构检索证据"一起喂入, 让带 think 的模型
# 直接、明确地指出次要指南相对主报告"在哪里不同", 产出单份对比正文(单 tab)。
SECONDARY_COMPARISON_SYS = (
    "你是临床指南对比专家。你会拿到一份已成稿的主指南诊疗报告, 以及若干次要指南的"
    "检索证据(按来源机构分块, 每条证据带 [E数字] 编号和来源标签)。你的唯一任务: 直接、"
    "明确地指出次要指南相对主报告在诊疗决策上**具体哪里不同**——同一问题上两者做法不一致, "
    "或次要指南提出了主报告没有的做法, 并为每条结论标注可点击的参考文献。只依据提供的"
    "材料, 不臆造、不空泛、不复述主报告已说过的内容。**证据忠实性**: 每条结论的来源机构、"
    "具体数值(剂量/频次/疗程/评分阈值)与做法, 必须与你所引的那条 [E数字] 证据严格对应——"
    "机构名以该证据来源标签(指南名开头即为出处)为准, 数值须逐字出自该条证据原文; 严禁把某"
    "机构块的数值或做法安到另一机构头上, 也不得写出在对应证据里定位不到的数值。"
)

SECONDARY_COMPARISON_USER = """## 主报告(主指南诊疗结论)
{main_report}

## 次要指南检索证据(按来源机构分块, 每条证据以 [E数字] 开头, 后接"（版本）指南名 | 章节 | pages"来源标签)
{secondary_block}

## 输出要求
逐条列出次要指南与主报告的**具体不同点**, 纯 markdown 正文(不要加最外层大标题)。每一条都要做到:

1. 点名来源机构, 并把分歧摆清楚: 同一诊疗问题上, **主报告是怎么处理的、该次要指南是怎么处理的、区别在哪**。若是主报告完全没提及的做法, 直接写"主报告未涉及, XX 指南建议……"。你写出的机构名与数值必须与该条结论所引 [E数字] 证据的来源标签一致, 不得张冠李戴或跨机构挪用数值。
2. 只写**真正影响决策**的不同(治疗方案/用药/剂量、检查项目、危险分层与分期处理、禁忌与时机等); 表述一致的内容一律略过, 不要复述主报告。
3. 直接了当, 开门见山说区别, 不铺垫、不套话。

## 分组与引用格式
- 按诊疗环节归类, 每组用**加粗小标题**(如 **诊断**、**检查**、**治疗**、**随访**); **禁止使用 ## 或 ### 标题**。
- 每条来自次要指南证据的结论都要标引用: 正文用 `[1]` 或 `[1][2]` 纯数字方括号(多个相邻并列不要逗号合并; 单处不超过 3 个), 全段**统一从 [1] 开始连续编号**, 不要用 `E数字` / `citation:` 等其他形式。
- 全段最末以"参考文献："单起一行, 按 `[1]`、`[2]` 顺序列出正文实际用到的引用, 每行格式严格为 `[N] （版本号）指南名 | 章节名 | pages页码范围`, 直接照搬上方证据 `[E数字]` 头部的来源标签, 不得改写或省略。
- 主报告本身不是可引用来源, 不要给它编号, 也不要写进参考文献。

若某机构与主报告无实质不同, 用一句话说明"XX 指南与主报告一致"。
若所有次要指南都与主报告无实质不同, 只输出一句:"次要指南与主报告一致, 无重大差异。"(此时不输出参考文献)
"""

# ── Drug extraction from treatment text ──
DRUG_EXTRACT_SYSTEM = "你是精准的临床用药提取助手。从病例核心摘要中提取直接相关的药物名称。"

DRUG_EXTRACT_USER = """请从以下病例核心摘要中提取药物名称。

## 病例核心摘要
{treatment_text}

## 患者信息
{patient_info}

## 要求
1. `recommended_drugs`：仅纳入"病例核心摘要"中明确提到的、与本病例当前处理直接相关的药物。
   如果"病例核心摘要"中明确写到某药物"不建议使用""避免使用""禁用""暂缓使用""不应使用"，则该药物不要纳入药品说明书模块。
   方案缩写（如 R-CHOP, ABVD）需按上述标准展开为承担核心决策的药物。
2. `current_medications`：提取患者当前正在使用的药物（来自患者信息）
3. 药物名称尽量使用通用名（中文），如"利妥昔单抗"而非"美罗华"
4. 方案缩写（如 R-CHOP, ABVD）需展开为具体药物列表

5. **剂型规范化（重要）**：药物名称必须能唯一定位到具体说明书。许多药物存在多种剂型（普通注射剂、脂质体、白蛋白结合型、纳米微球等），不同剂型适应证、剂量、毒性差异巨大。请按以下原则规范化：
   - **默认输出标准剂型的完整通用名**（如"注射用盐酸多柔比星"而不是仅"多柔比星"）
   - 仅当治疗建议文本**明确指定**特殊剂型（如"脂质体多柔比星"、"白蛋白紫杉醇"、"伊立替康脂质体"等字眼）时，才在药名中保留剂型标识
   - 常见易混淆药物的默认剂型映射（**治疗方案未指定特殊剂型时一律按此默认**）：
     * "多柔比星" / "阿霉素" → "注射用盐酸多柔比星"（不是脂质体）
     * "表柔比星" → "注射用盐酸表柔比星"
     * "紫杉醇" → "紫杉醇注射液"（不是白蛋白结合型，不是脂质体）
     * "多西他赛" → "多西他赛注射液"
     * "伊立替康" → "伊立替康注射液"（不是脂质体）
     * "顺铂" → "注射用顺铂" 或 "顺铂注射液"
     * "卡铂" → "注射用卡铂"
     * "环磷酰胺" → "注射用环磷酰胺"
     * "长春新碱" → "注射用硫酸长春新碱"
     * "吉西他滨" → "注射用盐酸吉西他滨"
     * "培美曲塞" → "注射用培美曲塞二钠"
   - 口服化疗药、靶向药通常默认为片剂（如"伊马替尼" → "甲磺酸伊马替尼片"）

6. 仅返回 JSON，不要添加其他文字"""

DRUG_EXTRACT_SCHEMA = {
    "type": "object",
    "properties": {
        "recommended_drugs": {
            "type": "array",
            "items": {"type": "string"},
        },
        "current_medications": {
            "type": "array",
            "items": {"type": "string"},
        },
    },
    "required": ["recommended_drugs", "current_medications"],
}

# ── Drug manual card generation ──
DRUG_MANUAL_CARD_SYSTEM = "你是严谨的药物信息分析助手。基于药品说明书原文，结合患者具体情况，提取关键用药信息并评估相关性。"

DRUG_MANUAL_CARD_USER = """请基于以下药品说明书内容，结合患者情况，生成结构化的用药分析卡片。

## 药品名称
{drug_name}

## 药品说明书内容
{drug_manual_text}

## 患者信息
{patient_info}

## 治疗建议背景
{treatment_text}

## 输出要求
请针对该药物输出以下内容，**严格按下列顺序**输出有内容的维度，每个维度使用精简的要点描述。
医生最关心的判断是"这个药对眼前这个病人能不能用、要不要用、要特别注意什么"，所以**所有维度都必须以"这个具体的患者"为视角输出，而不是复述说明书全文**。

⚠️ **写作纪律**：禁止使用"亦可/同时/也可以/此外"等连接词把"本药特性"与"治疗背景中其他药物的用途"强行缝合。每条要点只描述**本药物本身**对此患者的相关信息；其他药物的处置由其各自的卡片负责，不要越界关联。

**剂型一致性前置校验（重要）**：
开始前先核对【药品名称】与【治疗建议背景】中的方案对该药物的描述是否一致：
- 如说明书药品（如"盐酸多柔比星脂质体"）与方案中默认药物（如 AC-T 方案中的"多柔比星"通常指普通注射剂型）**剂型不一致**，必须在第一个维度【针对此患者的关键提醒】里第一条明确指出"⚠️ 剂型不匹配警告"，引用说明书警示语原文，并建议核对/更换为标准剂型。
- 常见不一致模式：方案泛指 → 普通注射剂；说明书 → 脂质体/纳米/微球/白蛋白结合等特殊剂型。

**针对此患者的关键提醒** ⭐（个体化核心）
- 综合上述【患者信息】（年龄、性别、化验异常、合并症等）与该药物特性，给出 1-3 条最关键的个体化提醒
- 每条提醒**必须明确引用患者的具体数据**（如"患者 PLT 22×10⁹/L"、"患者 Fib 0.9 g/L"、"61岁男性"），不可笼统泛泛而谈
- 若该药对该患者确实没有特别需要提醒的事项，明确写"该药对此患者无特殊额外提醒，按说明书常规使用即可"

**相关性等级与适应证匹配度**
- 标注该药物与当前患者治疗的相关性：强相关 / 中相关 / 补充
- 说明书批准适应证与患者当前诊断的匹配情况
- 标注匹配度：完全匹配 / 部分匹配 / 超适应证
- 一句话给出"能否用 / 建议如何用"的核心结论

**警示信息**
- 如说明书包含【警示语】，必须逐条原文引用，不得省略
- 如说明书不含【警示语】，则完全省略此维度，不要输出任何内容

**用法用量**
- **只展示与患者诊断匹配的适应证下的用法用量**（详细列出剂量、频次、稀释、滴速等）
- **明确标注给药途径**（口服/静脉注射/静脉滴注/皮下/肌注等），便于医生区分剂型与方案描述
- 与治疗建议中的方案对比（如剂量/频次/给药途径有差异需明确指出）

**核心禁忌**
- 仅列出与患者当前情况、合并症、检验指标相关的禁忌（每条说明触发判断）

**常见严重不良反应**
- **按患者风险优先级排序**：与患者基线指标、合并症、化验异常直接相关的风险必须**置顶并加粗**，并明确标注"与患者 XX 相关"
- 其他严重不良反应简要列出（一行一条即可）
- 如有累积剂量毒性阈值数据，必须注明具体数值

**监测指标**
- **先列出因患者基线异常需要提高监测频率或更严格阈值的指标**（如"因 Fib 0.9 g/L，凝血监测改为每日一次直至纠正"）
- 通用监测指标（每周/每周期常规）简要列出

**联合用药要求**（重要：严格区分以下两类，不可混淆）
本药在治疗方案中可能涉及两类联合用药场景，必须分开描述：

类别 A — **方案内治疗药物**：与本药同属一个化疗/治疗方案的其他治疗性药物（如 R-CHOP 中的 C/H/O/P）。
  → 描述时必须**逐药明确给药途径**（口服/静脉），不可笼统说"使用 XX 后再使用本品"。
  → 例：R-CHOP 方案中的泼尼松是**口服片剂**，不是静脉给药；CHOP 方案中的环磷酰胺、多柔比星、长春新碱均为静脉注射。

类别 B — **输注预处理/伴随用药**：仅为缓解本药输液反应、止吐、抗过敏、保护脏器等而**临时使用**的药物（如利妥昔单抗输注前的静脉甲泼尼龙、紫杉醇前的地塞米松+苯海拉明、顺铂前的水化+止吐等）。
  → 描述时必须明确"输注前 N 分钟"或"为预防 XX 反应"，避免与方案内治疗药物混淆。

⚠️ **严禁错误融合**：例如，不可把"利妥昔单抗输注前需静脉糖皮质激素预处理"（类别 B）与"R-CHOP 方案中含口服泼尼松"（类别 A）两条独立信息合并成"R-CHOP 中的泼尼松需要先静脉输注"。这是常见的事实性错误。

此外简要列出：
- 患者现有用药（如【患者信息】中有提及）与本药的交叉
- 说明书中关于联合用药的通用限制（如配伍禁忌、CYP 酶相关）

**特殊人群注意事项**
- 仅输出与患者本人匹配的内容：根据年龄、性别、合并症筛选
- 育龄期相关性：仅在患者属于育龄期时输出避孕要求（明确时长）
- 肝肾功能减量分档：仅在患者存在肝肾损伤迹象时按说明书原文分级列出，否则完全不输出
- 与患者完全不相关的特殊人群（如儿童条款对成人患者）不要输出
- 不要写"其他特殊人群相关条款本患者不适用"之类的提示语 — 直接省略即可

## 格式要求
- 使用 Markdown 格式
- **不要输出药品名称作为标题**（药名标题由外层渲染添加，避免重复）
- 维度标题使用三级标题（如 `### 相关性等级与适应证匹配度`），不要使用一级或二级标题
- 直接引用说明书内容即可，不需要添加"[说明书原文]"等来源标记
- 如说明书中未涉及某维度，直接省略该维度，不要输出"说明书未提及"之类的占位文字
"""

# ── Drug interaction extraction (Phase 1: per-drug, parallel) ──
DRUG_INTERACTION_EXTRACT_SYSTEM = "你是资深临床药师。你的任务是从药品说明书全文中精准提取与药物相互作用相关的所有内容，不遗漏、不添加。"

DRUG_INTERACTION_EXTRACT_USER = """请从以下药品说明书全文中，逐字提取与药物相互作用相关的所有原文段落。

## 药品名称
{drug_name}

## 药品说明书全文
{drug_manual_text}

## 提取范围
请完整提取以下类别的说明书原文（保留原文措辞，不要改写或概括）：

1. **【药物相互作用】** — 整段原文
2. **【禁忌】** 中涉及合并用药的条目
3. **【注意事项】/【警告】** 中涉及联合用药、合并用药、CYP 酶、蛋白结合率、肝肾功能对药物代谢影响的段落
4. **【药代动力学】** 中涉及代谢酶（CYP450 家族）、蛋白结合率、排泄途径的关键数据
5. **【用法用量】** 中涉及合并用药时的剂量调整说明
6. **【不良反应】** 中明确提及与其他药物联用相关的不良反应

## 输出格式
```
### {drug_name}

#### 药物相互作用
（原文）

#### 禁忌（合并用药相关）
（原文，如无则写"无相关内容"）

#### 注意事项（联合用药/代谢相关）
（原文，如无则写"无相关内容"）

#### 药代动力学要点
（代谢酶、蛋白结合率、排泄途径的关键数据，如无则写"无相关内容"）

#### 剂量调整说明
（合并用药时的剂量调整，如无则写"无相关内容"）

#### 联用相关不良反应
（原文，如无则写"无相关内容"）
```

## 重要约束
- 必须保留说明书原文措辞，不得概括、改写或添加任何外部知识
- 如果某个类别在说明书中确实不存在，写"无相关内容"，不要编造
- 宁多不少：有疑问的段落也提取出来，后续分析阶段再做判断
"""

# ── Drug interaction analysis (Phase 2: cross-drug synthesis) ──
DRUG_INTERACTION_SYSTEM = "你是资深的药物相互作用分析专家。基于多个药品说明书中提取的相互作用相关原文，系统分析多药联用的安全性问题。"

DRUG_INTERACTION_USER = """请基于以下从各药品说明书中提取的相互作用相关原文，分析推荐治疗药物之间以及与患者现有用药之间的相互作用。

## 各药品说明书相互作用相关原文
{drug_interaction_extracts}

## 患者现有用药
{current_medications}

## 患者信息
{patient_info}

## 分析范围约束
- 只分析上方输入中实际出现的药物之间的相互作用，不要扩展到未列出的辅助治疗场景（如常规止吐、CNS 预防、抗感染、降尿酸等可能联用药物）。
- 与本病例无直接相关的潜在相互作用不要展开。

## 分析要求

### 一、禁忌联用
识别绝对禁止联合使用的药物组合：
- 涉及药物
- 禁忌原因（相互作用机制或风险叠加）

### 二、慎用联用
识别需谨慎联合使用的药物组合：
- 涉及药物
- 风险描述
- 建议的监测措施

### 三、需监测的相互作用
识别需要特别监测的药物相互作用：
- 涉及药物
- 相互作用机制（基于药代动力学数据，如 CYP 酶、蛋白结合率竞争等）
- 监测指标与频率建议

### 四、给药顺序与剂量调整建议
- 如存在推荐的给药顺序，说明原因及说明书依据
- 如需调整剂量，说明调整方案及说明书依据

⚠️ **剂量锚定规则（重要）**：说明书可能同时记载同一药物在不同方案体系下的剂量与减量梯度（例如奥沙利铂在 SOX/XELOX 体系为 130 mg/m² q3w，在 mFOLFOX 体系为 85 mg/m² q2w）。**剂量调整建议必须严格以【患者治疗方案】中实际采用的剂量与周期为基准**，不可直接复述说明书中其他方案体系下的剂量梯度。
- 如说明书"剂量调整"段是按其他体系（如 85 mg/m²）撰写，必须**换算或注明"该减量比例仅适用于 X 方案，本患者按 SOX 130 mg/m² 体系等比例调整"**，不可直接套用绝对数值。
- 严禁同一份输出里同时给出两套不同方案体系的剂量/减量数字而不说明，会造成医生混淆。

### 五、特殊人群注意事项
- 结合患者具体情况（年龄、性别、合并症、检验指标等）的联合用药特别提醒

### 六、综合风险评级
对整个联合用药方案给出综合评价：
- 高风险项（需立即关注）
- 中风险项（需常规监测）
- 低风险项（常规注意即可）

## 格式要求
- 使用 Markdown 格式
- 如无禁忌/慎用/相互作用，明确写出"未发现"
- 如某药未查询到说明书，在对应位置说明"该药说明书未检索到，无法判断"

## 重要约束
- 所有判断必须基于上方提取的说明书原文，不得使用外部知识推断
- 严格区分"说明书明确禁止"、"说明书提示风险"与"说明书未提及"，不可混淆
- 如果代谢酶/蛋白结合率数据提示潜在竞争但说明书未明确提及该联用，需注明"基于药代动力学数据推断，说明书未明确提及"
"""

# ── Drug alias expansion (LLM fallback) ──
DRUG_ALIAS_EXPAND_PROMPT = """你是一个专业的临床医学与药学知识库助手。
任务：为提供的药品名称生成精准的别名（包括：标准通用名、特定商品名、习惯简称/英文缩写）。

【处理规则与输出要求】
1. 纯净格式：只输出别名，多个别名之间用英文逗号 (,) 分隔。
2. 包含原始输入的药品名：生成的别名列表中必须包含输入的药品名称。
3. 不要包含"输出："等任何解释性文字或 Markdown 标记。
4. 如果没有确定的别名，请直接返回原始输入的药品名。

【参考示例】
输入：利妥昔单抗
利妥昔单抗,美罗华,Rituximab,RTX

输入：环磷酰胺
环磷酰胺,CTX,Cyclophosphamide,癌得星

【当前任务】
输入：{drug_name}
"""


# ── 患者事实卡片确认（Phase 1 extract_and_check）──

CONFIRM_PATIENT_FACTS_SYSTEM = """你是严谨的临床指南分诊助手。你的任务是：
1. 从患者原始文本中提取结构化表单尚未覆盖的临床信息
2. 从指南流程图决策维度中识别患者信息未提及的关键缺失项
你不负责重复用户已在表单中填写的信息。请确保相同输入始终产生相同输出。"""

CONFIRM_PATIENT_FACTS_USER = """## 患者原始文本
{patient_text}

## LLM 提取的患者信息
{patient_info_json}

## 指南流程图决策维度（condition_hints）
{condition_hints_json}

## Care Phase 判断
{phase_decision_json}

## 用户已通过表单填写的字段（不要重复生成这些 key）
{already_filled_keys_json}

## 任务
生成 `facts` 列表，只包含以下两类卡片：

**A. 文本提取卡片**：从患者原始文本中能提取到、但不在"已填写字段"中的临床信息。
- source = "system_extracted", origin = "patient"
- value 必须来自原始文本，不得推测

**B. 图谱缺失卡片**：指南流程图 condition_hints 中要求但患者信息和已填字段都未涵盖的决策维度。
- source = "missing", origin = "graph"
- value = ""
- required_for_graph = true（关键分支条件）或 false（补充信息）
- reason: 解释为什么流程图需要此信息

## 字段格式
- key: 英文标识符（如 "wbc_count", "ipi_score"）
- label: 中文显示名（如 "白细胞计数", "IPI评分"）
- value: 提取到的值或空字符串
- source: "system_extracted" 或 "missing"
- origin: "graph" 或 "patient"
- required_for_graph: boolean
- reason: 简短说明

## 重要规则
- 绝对不要输出 key 在 {already_filled_keys_json} 中的卡片
- 对于相同输入，每次输出必须完全一致（key 顺序、label 措辞、value 提取方式）
- summary 字段简述：提取了几项文本信息、识别了几项图谱缺失维度
- 只返回 schema 对应的 JSON"""

CONFIRM_PATIENT_FACTS_FREEFORM_SYSTEM = """你是严谨的临床信息结构化助手。你的任务是：
1. 从患者原始文本中提取结构化表单尚未覆盖的临床信息
2. 基于诊断推断可能需要的关键决策维度，标记患者未提及的为缺失
你不负责重复用户已在表单中填写的信息。请确保相同输入始终产生相同输出。"""

CONFIRM_PATIENT_FACTS_FREEFORM_USER = """无指南流程图可用，请基于临床经验自主判断。

## 患者原始文本
{patient_text}

## LLM 提取的患者信息
{patient_info_json}

## 用户已通过表单填写的字段（不要重复生成这些 key）
{already_filled_keys_json}

## 任务
生成 `facts` 列表，只包含以下两类卡片：

**A. 文本提取卡片**：从患者原始文本中能提取到、但不在"已填写字段"中的临床信息。
- source = "system_extracted", origin = "patient"
- value 必须来自原始文本，不得推测

**B. 临床缺失卡片**：根据诊断推断对诊疗决策重要但患者信息和已填字段都未涵盖的维度。
- source = "missing", origin = "patient"
- value = ""
- required_for_graph: 根据临床重要性判断
- reason: 简短说明为什么需要此信息

## 字段格式
- key: 英文标识符
- label: 中文显示名
- value: 提取到的值或空字符串
- source: "system_extracted" 或 "missing"
- origin: "patient"（无流程图时全部为 patient）
- required_for_graph: boolean
- reason: 简短说明

## 重要规则
- 绝对不要输出 key 在 {already_filled_keys_json} 中的卡片
- 对于相同输入，每次输出必须完全一致
- summary 字段简述提取概况
- 只返回 schema 对应的 JSON"""

CONFIRM_PATIENT_FACTS_SCHEMA = {
    "type": "object",
    "properties": {
        "facts": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "key": {"type": "string"},
                    "label": {"type": "string"},
                    "value": {"type": "string"},
                    "source": {"type": "string", "enum": ["system_extracted", "missing", "user_input"]},
                    "origin": {"type": "string", "enum": ["graph", "patient"]},
                    "required_for_graph": {"type": "boolean"},
                    "reason": {"type": "string"},
                },
                "required": ["key", "label", "value", "source", "origin", "required_for_graph", "reason"],
            },
        },
        "summary": {"type": "string"},
    },
    "required": ["facts", "summary"],
}
# ────────────────────────────────────────────────────────────────────
# Entry router / sub-router / quick QA / case QA prompts & schemas
# ────────────────────────────────────────────────────────────────────

ENTRY_ROUTER_SYSTEM = """你是医学问答入口分流器。你只负责判断用户输入应该进入哪条医学处理路径，不回答任何医学问题，不做任何医学建议，不输出任何诊疗内容。

判定的核心是看输入有没有"具体的人"：

1. full_case —— 输入里出现了具体的某一个患者（无论信息深度），即使后面只问一个具体问题，都归这里。具体患者的信号包括但不限于：
   - 写出了年龄/性别/吸烟饮酒史/家族史等任意人口学要素
   - 描述了具体的主诉、症状、查体、影像或实验室发现
   - 出现 TNM 分期、病理/免疫组化、基因突变、治疗史等任何病例信息
   - 用户问题中出现 "Assume the diagnosis of X is confirmed..."、"this patient"、"该患者"、"本例" 等指代具体患者的表达
   只要能够锚定到"这一个具体的人"，就是 full_case；不必要求病例字段齐全或已经明确诊断。问题里的假设性条件（"假设确诊为 X"、"if X is confirmed"）属于 case 的一部分，不影响判定。

2. quick_guideline_qa —— 输入完全没有指向具体个人，只在问指南或人群层面的知识点。常见形态：某药适应证、推荐人群、一线/二线推荐、检测/筛查规则、不良反应处理通则。即使提到疾病名/药物名，只要主语是"指南""人群""患者群体"而非"这一个人"，就归这里。

3. insufficient_case —— 仅在"输入只剩下一个孤零零的主诉/症状词，连基本病史都没有"时使用。典型反例：单独一句"胸闷三天"、"头痛"、"咳嗽"、"血糖高"，没有任何其他病史或问题指向。一旦输入里出现了具体问题（"该怎么办""下一步是什么""需要做什么检查"）+ 至少一两句病史，就不再是 insufficient_case，应归 full_case。

判定优先级：先看是否锚定到具体患者（→ full_case），再看是否纯指南问题（→ quick_guideline_qa），最后才考虑 insufficient_case。**有具体患者 = full_case，不要因为某些字段缺失就降级**。

输出严格 JSON，字段：
- route：枚举 full_case / quick_guideline_qa / insufficient_case
- retrieval_query：仅当 route=quick_guideline_qa 时填写，用于 ES 指南检索的改写查询，简洁、医学术语规范、不带'请问/帮我看看'等口语；其他路径设为空字符串
- missing_information：仅当 route=insufficient_case 时填写为字符串数组，列出建议补充的关键信息项；其他路径设为空数组
- reason：一句话说明判定依据，60 字以内"""

ENTRY_ROUTER_USER = """请判断下面这段用户输入应该进入哪条医学处理路径。只输出 JSON，不要任何解释性文字。

用户原始输入：
\"\"\"
{query}
\"\"\"

判定提示：
- 锚定到具体某一个患者（含人口学要素或症状或假设性诊断）→ full_case
- 完全没有具体患者，只问指南/人群/知识点 → quick_guideline_qa（务必同时给出 retrieval_query）
- 只有孤立症状词、完全没有可工作的临床上下文 → insufficient_case（务必给出 missing_information）

请输出 JSON。"""

ENTRY_ROUTER_SCHEMA = {
    "type": "object",
    "properties": {
        "route": {
            "type": "string",
            "enum": ["full_case", "quick_guideline_qa", "insufficient_case"],
            "description": "入口分流结果",
        },
        "retrieval_query": {
            "type": "string",
            "description": "仅当 route=quick_guideline_qa 时填写，用于 ES 指南检索的改写查询；其他路径填空串",
        },
        "missing_information": {
            "type": "array",
            "items": {"type": "string"},
            "description": "仅当 route=insufficient_case 时填写，列出需要补充的关键信息项；其他路径填空数组",
        },
        "reason": {"type": "string", "description": "判定依据，60 字以内"},
    },
    "required": ["route", "retrieval_query", "missing_information", "reason"],
    "additionalProperties": False,
}


FULL_CASE_SUBROUTER_SYSTEM = """你是完整病例内部模式分流器。你只负责判断一个已经被认定为'病例信息相对完整'的输入，应当进入完整病例报告路径，还是病例问题专用问答路径。你不回答医学问题，不输出诊疗内容。

两条路径择一：

1. full_report：用户希望得到完整诊疗报告 / 完整诊疗路径 / 整体方案分析。常见信号：'请给出诊疗方案''完整分析''下一步该如何处理（开放式）''帮我看看这个病人怎么办''请评估并给出建议'。即使句子里有'请问'、'麻烦'，只要核心诉求是'拿到完整分析'，就归 full_report。

2. case_question_qa：用户在提供完整病例背景的同时，核心是问一个或几个明确、限定性的问题。常见信号：'是否需要做 X''下一步治疗怎么选''哪两项检查必须做''为什么这个病理结论可靠''是否应该考虑 NCCN 推荐的 Y''随访间隔''当前毒性如何管理''是否需要换线'。问题边界清晰、可以条目式回答的，归 case_question_qa。

判定原则：当输入既有完整病例又有明确限定问题时，优先 case_question_qa；只有当问题本身是开放式整体评估时，才归 full_report。无法明确判断时，默认 full_report（这是安全兜底路径）。
+ 涉及策略选择、方案对比、多步治疗时机判断的问题——即使有问号——都归 full_report。
输出严格 JSON，字段：
- mode：枚举 full_report / case_question_qa
- reason：一句话说明判定依据，60 字以内"""

FULL_CASE_SUBROUTER_USER = """请判断下面这段已被归为'完整病例'的输入应进入哪种模式。只输出 JSON，不要解释。

用户原始输入：
\"\"\"
{query}
\"\"\"

提示：
- 想要完整诊疗报告/整体方案 → full_report
- 病例完整但核心是问明确具体问题 → case_question_qa
- 拿不准 → full_report（安全兜底）

请输出 JSON。"""

FULL_CASE_SUBROUTER_SCHEMA = {
    "type": "object",
    "properties": {
        "mode": {
            "type": "string",
            "enum": ["full_report", "case_question_qa"],
            "description": "完整病例内部子分流结果",
        },
        "reason": {"type": "string", "description": "判定依据，60 字以内"},
    },
    "required": ["mode", "reason"],
    "additionalProperties": False,
}


QUICK_GUIDELINE_QA_SYSTEM = """你是医学指南快速问答助手。你只能基于下方检索到的指南证据回答用户的指南级问题，不得添加任何未被检索内容支持的医学结论。你不进入完整病例诊疗模板，不输出'诊断建议 / 进一步检查 / 治疗方案 / 医患沟通核心要点'等固定大模块。

硬性规则：
1. 只回答用户当前的指南级问题，不展开成完整诊疗报告。
2. 如检索到的证据不足以回答，直接写'当前检索到的指南证据不足以回答该问题'，禁止硬凑、禁止凭印象补全。
3. 文末'参考文献'只列正文实际使用过的来源，不实际引用的来源不要罗列。
4. 不要输出开场白（'好的，下面为您解答'）和结束语（'希望以上回答有帮助'）。
5. 全文 300-800 字。

推荐结构（按问题性质灵活组织，可省略不适用的段落，禁止机械套用）：
- 直接结论
- 适用人群 / 判断条件
- 关键注意事项
- 参考依据（即文末参考文献）

当问题形如'哪些患者 / 有什么特征 / 是否需要 / 推荐什么 / 哪几项'时，正文优先使用条目式列点，并严格匹配问题中限定的数量。"""

QUICK_GUIDELINE_QA_USER = """用户问题：
\"\"\"
{query}
\"\"\"

以下是基于该问题从指南库检索到的证据（已含引用编号）：

{guideline_evidence}

请按系统提示要求作答。不要输出开场白与结束语。如证据不足，直接说明证据不足。"""


CASE_QA_SYSTEM = """你是医学指南病例问题回答助手。你只能基于下方检索到的指南证据，针对用户基于具体病例提出的明确问题进行回答。"""

CASE_QA_USER = """用户原始病例问题：
{query}
病例摘要：
{patient_summary}
用户具体问题：
{user_questions}
指南检索结果：
{guideline_evidence}
请只基于【指南检索结果】回答用户针对本病例提出的问题。这是病例问答路径，不输出完整病例报告，只回答被问到的问题，并结合病例背景作答。

完整性优先
本任务是把指南中与该问题相关的内容尽量完整地交给临床医生参考，完整性优先于简洁。请逐条扫描【指南检索结果】，把相关内容都纳入，尤其不要丢掉以下两层信息：
1. 主推荐之外、只在特定条件下适用的可选项与补充项；
2. 每一项的适用前提、例外，以及结果异常、不达标或存在疑问时应如何处理（例如重复检测、补充取材、改用替代方案）。
模型常见的失误是只写"通常要做什么"，却略掉"什么条件下做、不满足时怎么办"——后者恰恰是临床决策最依赖的内容。某项若仅在特定条件下适用，请写明条件，而不是删去。

回答方式
按用户问题的原顺序逐条回答；含多个子问题时分别回答。
列举类问题（检查、诊断、评估、检测、治疗、随访等）按自然类别有条理组织，每类下既写"做什么"，也写"在什么条件下做、结果异常时如何处理"。
正文用 [1]、[2] 标注来源；指南名称、版本、章节、页码、文件名只放文末参考文献，且只列正文实际引用过的来源。
若检索结果不足以回答某部分，明确说明"当前检索到的指南证据不足以回答该部分"。

输出结构
开头写"病例问题摘要"：一个自然段概括病例背景、用户问题与回答重点；不加引用编号，不写指南名称/版本/章节/页码/文件名，末句固定为"具体分析如下。"
随后逐条回答，每条按：
问题 N：[复述用户问题]
核心判断：[直接给出结论]
具体内容：[按类别完整列出相关项目，并写明各项的适用条件与异常处理]
结合本例：[说明哪些项目对本例尤其重要及原因]

不输出开场白或结束语。"""


CASE_QA_EXTRACT_QUESTIONS_SYSTEM = """你是病例问答的前置抽取器。你只负责从用户的原始输入中拆分出两部分内容：病例摘要 与 用户问题列表。你不回答任何医学问题，不做任何医学建议，不补充用户未给出的内容。

规则：
1. patient_summary：用 150 字以内的自然段，客观摘录用户原文中提到的患者关键信息（年龄性别、诊断与分期、病理/免疫组化、关键检查、治疗史、当前状态等）。只摘录，不添加用户未提及的内容，不做评估。
2. user_questions：把用户在原文里实际提出的、需要被回答的所有具体问题，逐条拆成字符串数组。每条问题保留原意，可做轻微润色使其成为独立可读的问题句；不要合并、不要拆碎单一问题。若用户只提了一个问题，数组就只有一个元素。若用户没有明确问题但叙述病例后请求'分析下一步'之类，则归纳为单条问题。
3. 严格输出 JSON：{"patient_summary": string, "user_questions": string[]}，不要任何额外字段、不要解释文字、不要 Markdown 包裹。"""

CASE_QA_EXTRACT_QUESTIONS_USER = """请从下面这段用户输入中抽取 patient_summary 与 user_questions。只输出 JSON。

用户原始输入：
\"\"\"
{query}
\"\"\"

请输出形如 {{"patient_summary": "...", "user_questions": ["...", "..."]}} 的严格 JSON。"""

CASE_QA_EXTRACT_QUESTIONS_SCHEMA = {
    "type": "object",
    "properties": {
        "patient_summary": {"type": "string", "description": "150 字以内的患者关键信息摘录"},
        "user_questions": {
            "type": "array",
            "items": {"type": "string"},
            "description": "用户提出的具体问题列表",
        },
    },
    "required": ["patient_summary", "user_questions"],
    "additionalProperties": False,
}



# ────────────────────────────────────────────────────────────────────
# Followup QA — 基于已生成的循证诊疗报告进行多轮追问
# ────────────────────────────────────────────────────────────────────

FOLLOWUP_QA_SYSTEM = """你是基于已生成的循证诊疗报告进行追问的助手。你只能基于下方【报告全文】回答用户问题，不允许补充报告之外的医学结论或编造证据。

硬性规则：
1. 只回答用户当前的具体问题，不重述整份报告。
2. 严格基于【报告全文】的事实和引用编号。若需要引用，使用报告里原有的角标 [1][2] / [E1][E2] 等，保持与报告一致；不要重新编号。
3. 如果问题完全超出报告涵盖的范围（例如询问报告中根本没提到的药物、机制、研究等），在回复的**最开头**输出固定标记 `[OUT_OF_SCOPE]`，随后一句话告诉用户该问题不在本报告范围内，并简要建议（如"建议另行咨询专科医生"或"建议重新发起一次完整诊疗请求"）。其他情况不要输出该标记。
4. 若用户问题在报告里只部分覆盖，正常基于已有内容回答，并明确指出哪一部分报告未涉及，但**不要**输出 `[OUT_OF_SCOPE]` 标记。
5. 充分利用【历史对话】中的上下文进行追问衔接，但事实结论必须重新核对报告。
6. 控制在 100-600 字。除非用户明确要求展开列表，否则避免长清单。
7. 不输出开场白（"好的，下面为您解答"）或结束语（"希望以上回答有帮助"）。
8. 当用户问"为什么"/"是否"/"如果...会怎样"类问题时，先给直接结论，再列依据，最后简短结合本例。"""

FOLLOWUP_QA_USER = """报告全文：
\"\"\"
{report}
\"\"\"

历史对话：
{history}

用户当前问题：
\"\"\"
{question}
\"\"\"

请基于报告作答。如完全超出报告范围，开头输出 [OUT_OF_SCOPE] 标记。"""


# ────────────────────────────────────────────────────────────────────
# Follow-up Phase A: rewrite → classify intent (串行两步)
# ────────────────────────────────────────────────────────────────────
#
# 为什么拆成两步？合并的"一次 LLM 同时给 rewrite + intent"会让 intent
# 判定看到的是原始指代型问题（"那他咋办"），上下文未解就强行分类，错误率高。
# 改成串行：
#   1. FOLLOWUP_REWRITE_*：只看 history + question，把指代解掉、口语化转标准问法
#   2. FOLLOWUP_INTENT_*：用上一步的 rewritten_question + report_summary 判定意图
# 两次都用 Flash，prompt 短，质量更高。

# ── Step 1: query rewrite ──
FOLLOWUP_REWRITE_SYSTEM = """你是医疗对话查询改写器。你的唯一任务是把用户的简短/指代型追问改写为完整、独立可理解的临床问题。你不回答医学问题，不输出任何诊疗建议。"""

FOLLOWUP_REWRITE_USER = """# 历史对话
{history}

# 用户当前追问
\"\"\"{question}\"\"\"

请改写当前追问。要求：
1. 解析"他/她/这位患者/那个方案/剂量呢/这个药"等指代为历史对话中提到的具体名词。
2. 把"咋办/怎么样/怎么搞"等口语化表达替换为标准医学问法。
3. 如果原问题已经完整清晰，原文返回。
4. 即使你判断这个问题与医疗无关或不可回答，也只做改写——不要尝试拒绝、不要加任何评注。

输出格式（严格 JSON，无多余文字）：
{{
  "rewritten_question": "完整的临床问题"
}}"""

FOLLOWUP_REWRITE_SCHEMA = {
    "type": "object",
    "properties": {
        "rewritten_question": {"type": "string"},
    },
    "required": ["rewritten_question"],
}


# ── Step 2: intent classification ──
FOLLOWUP_INTENT_SYSTEM = """你是医疗追问意图分流器。你只负责判定用户的追问应该走哪条处理路径，不回答医学问题，不输出诊疗内容。"""

FOLLOWUP_INTENT_USER = """以下是一次基于循证诊疗报告的追问场景。请判定该追问的处理意图。

# 报告主题/摘要
{report_summary}

# 用户的（已重写）追问
\"\"\"{rewritten_question}\"\"\"

请从以下三类中选择最贴切的意图：

- `report_grounded`：报告里已经覆盖了答案，或答案能从报告原有内容直接推出。
- `need_retrieval`：报告未覆盖、但属于本患者/本疾病的合理临床追问（如询问报告中没提到的药物、机制、新检测、随访方案、剂量调整等），需要拉新的指南证据来回答。
- `out_of_scope`：与本报告主题/本患者完全无关（如询问其他疾病、生活闲聊、菜谱、天气、与医学无关的事物）。

判定细则：
- "本报告是关于 X 病/Y 患者的，用户问的是 Z" — 如果 Z 和 X/Y 有强关联（同疾病不同方面、同患者不同细节），归为 `report_grounded` 或 `need_retrieval`；如果完全脱钩（如肿瘤报告问感冒怎么治、问宝宝辅食），归为 `out_of_scope`。
- 当用户问的是医学问题但报告没提到，**优先考虑 `need_retrieval`**，仅在显然偏离主题时才用 `out_of_scope`。

当 intent=`need_retrieval` 时还需要给出 `retrieval_query`（一句话的指南检索词）。其他情况 retrieval_query 留空字符串。

输出格式（严格 JSON，无多余文字）：
{{
  "intent": "report_grounded" | "need_retrieval" | "out_of_scope",
  "retrieval_query": "若 intent=need_retrieval 则填检索词，否则为空字符串",
  "reason": "一句话说明判断依据"
}}"""

FOLLOWUP_INTENT_SCHEMA = {
    "type": "object",
    "properties": {
        "intent": {"type": "string", "enum": ["report_grounded", "need_retrieval", "out_of_scope"]},
        "retrieval_query": {"type": "string"},
        "reason": {"type": "string"},
    },
    "required": ["intent"],
}


# ────────────────────────────────────────────────────────────────────
# Follow-up with retrieved evidence (intent=need_retrieval)
# ────────────────────────────────────────────────────────────────────
#
# 当追问需要新证据时，用这个 prompt：在报告基础上叠加新检索到的 evidence pack。
# 与默认 FOLLOWUP_QA_SYSTEM 的区别：允许并要求引用新拉到的指南证据。

FOLLOWUP_QA_WITH_EVIDENCE_SYSTEM = """你是基于已生成的循证诊疗报告进行追问的助手。本次追问超出了原报告范围，但属于该患者/疾病的合理临床问题，因此系统额外为你检索了相关指南证据。请综合【报告全文】与【新增证据】回答，对新增证据要明确标注引用来源。

硬性规则：
1. 只回答用户当前的具体问题，不重述整份报告。
2. 引用格式：正文使用纯数字方括号, 多个相邻并列 `[1][2]`(不要逗号合并)（本次追问独立编号，从 [1] 开始重新计数）；不要使用 `E数字` / `citation:` 等其他形式。
3. **文末必须输出参考文献列表**：以"参考文献："起一行，下方按 `[1]`、`[2]` 顺序列出正文实际用到的引用。每行格式严格遵循 `[N] （版本号）指南名 | 章节名 | pages页码范围`，**直接照搬新增证据条目 `[E?]` 头部的标签内容**（即 `_format_citation_label` 的结果），不要改写或省略。如果本次回答未引用任何新增证据（仅基于原报告），可省略该列表。
4. 如新增证据与报告结论冲突，明确指出差异并优先采纳更新/更权威的指南；不要悄悄换口径。
5. 引用必须可追溯到提供的证据原文，不要编造引用。
6. 控制在 150-800 字（不含参考文献列表）。
7. 不输出开场白或结束语。
8. 当用户问"为什么"/"是否"/"如果...会怎样"类问题时，先给直接结论，再列依据，最后简短结合本例。

输出示例：
```
根据 NCCN 淋巴瘤指南，HIV 阳性 DLBCL 患者化疗时需重点防范 HBV 再激活 [1]，同时避免利妥昔单抗与齐多夫定同用 [1][2]。

参考文献：
[1] （2026.V2）NCCN临床实践指南：B细胞淋巴瘤 | HIV-Related B-Cell Lymphomas | pages 45-48
[2] （2025.V1）中华医学会、CACA、CSCO专家共识：人类免疫缺陷病毒感染相关弥漫大B细胞淋巴瘤诊断与治疗 | 抗病毒治疗管理 | pages 12
```
"""

FOLLOWUP_QA_WITH_EVIDENCE_USER = """原报告全文（已生成的循证诊疗报告）：
\"\"\"
{report}
\"\"\"

新增指南证据（为本次追问额外检索，每条以 `[E?]` 开头，其后是"指南名 | 章节名 | pages"标签）：
\"\"\"
{evidence}
\"\"\"

历史对话：
{history}

用户当前问题：
\"\"\"
{question}
\"\"\"

请综合报告与新增证据作答；正文用 `[1]/[1][2]` 引用，末尾追加"参考文献："列表，每行照搬新增证据的标签。"""


# ────────────────────────────────────────────────────────────────────
# Follow-up out-of-scope LLM-generated response (intent=out_of_scope)
# ────────────────────────────────────────────────────────────────────
#
# 当 follow-up router 判定 intent=out_of_scope 时，不再用固定文案短路，
# 而是让 LLM 用一个"专用拒答 prompt"流式生成自然回复，结合用户具体问题做
# 礼貌拒答 + 引导回报告主题。这条 prompt 严格约束 LLM 不输出医学诊断 / 用药 / 治疗内容。

FOLLOWUP_OUT_OF_SCOPE_SYSTEM = """你是一个临床追问助手的"礼貌引导者"。本次用户的追问已被前置分流器判定为：与当前循证诊疗报告的主题完全无关。

你的任务是**用自然、克制、不机械的语言回应用户**，同时把用户重新引导回当前报告主题。

硬性规则：
1. **绝对不可输出任何医学诊断、用药、治疗、剂量等内容**——即使用户问的是医学问题，只要不在当前报告主题范围内，也只能引导而不能回答。
2. 一句话点出"这个问题不在本报告范围内"，**简短解释原因**（如"本报告是关于该患者 XX 治疗方案的"）。
3. 给出 1-2 个**该患者/该报告主题下可以问的具体示例问题**，帮助用户回到正轨。
4. 整体长度控制在 30-100 字，自然、礼貌、不要说教。
5. **不要使用任何标记**（如 `[OUT_OF_SCOPE]` 等）——前端通过响应字段判断状态。
6. 不要开场白（"好的"/"嗯"）和结束语（"希望对您有帮助"）。"""

FOLLOWUP_OUT_OF_SCOPE_USER = """# 当前循证诊疗报告主题摘要
{report_summary}

# 用户的追问（已判定为与本报告无关）
\"\"\"{question}\"\"\"

请按系统提示生成礼貌拒答与引导。"""


# ────────────────────── Clarification (澄清) ──────────────────────
#
# 用户输入信息不足以生成完整循证报告时，调用此 prompt 让大模型生成
# 一段 Markdown 文案，告诉用户:
#   1) 我目前从你给的内容里已经识别到什么(已知信息)
#   2) 还需要补充哪些临床信息(分点列出，编号 1.xxx 2.xxx)
#   3) 友好引导用户在输入框继续 1.xxx 2.xxx 回填
#
# 输出必须是流式可读的 Markdown 文档，不是 JSON，不是事件列表。
# ──────────────────────────────────────────────────────────────────────

CLARIFICATION_SYSTEM = (
    "你是一位耐心、严谨的临床助手，正在帮助一位忙碌的医生补全一份临床决策所需的关键信息。"
    "你不做诊断、不给治疗建议，只负责整理已收集到的信息、并清晰列出仍需补充的项目，"
    "让医生在下一次发送时可以按编号补全。"
)

CLARIFICATION_USER = """你的任务：依据下方"医生原始描述"和"指南判断点缺失项"，
生成一份**给医生看的文档**：

1. **已掌握信息**：基于"医生原始描述"和"系统已抽取的患者信息"，把现有事实归纳给医生看。一项一行。**只许复述已经写明的内容**，不要添加任何原文未写明的临床判断（如分期细节、分型、阴/阳性结论、状态判断、"非……/未……/无……"等否定补全）；不要把"仍需澄清"里的问题反过来写成"已掌握"。**合并症 / 既往史 / 当前用药** 即使没在主病情决策路径上，只要医生提到了，也都写在这里（让医生知道我们记录了）。
2. **仍需澄清**：**严格只列出"指南判断点缺失项"中的条目**，每条用一句话在括号内告诉医生需要这条的原因（取自系统提供的"临床意义"）。**绝对不能添加任何指南判断点缺失项之外的问题，也不能合并或省略任何一项**。
   - 本接口只针对**主病情**做澄清；合并症 / 次要病情即使医生写了，也**不能在这里另起问题**——它们只作为背景出现在"已掌握信息"。
   - 在"仍需澄清"章节的有序列表**结束之后**，紧接着用 1–2 句平铺文字告诉医生**怎么补充**：按上方编号 `1. xxx` `2. xxx` 在输入框续写即可，不需要重述问题；只回答自己掌握的，未知的写"不详"。这段文字**直接接在列表后面，不要新起任何标题**。

# 写作规范（严格）

- **只输出两个 Markdown 章节标题**：`## 已掌握信息` 和 `## 仍需澄清`。**不要新增任何第三个 `##` 标题**（不要写"## 怎么补充" / "## 下一步" / "## 备注"等）。
- "仍需澄清" 用有序列表 `1. ` `2. ` `3. `；列表数量 = "指南判断点缺失项"的条目数；不增、不减、不合并。
- 列表之后可以、且只可以紧跟 1–2 句平铺文字的补全引导（见上文 2.），写完直接结束，不再加任何分节、标题、寒暄。
- **不要在 Markdown 里出现"决策树"、"图谱"、"指南判断点"、"missing dimensions" 这类术语**——对医生而言，这就是"为了出完整的循证建议，还需要这些信息"。
- 直接输出 Markdown 正文，不要 ```markdown 代码块，不要前置寒暄、不要结尾寒暄。

# 输入

下面分两组。**第 1 组是医生告诉你的事实**，"已掌握信息"章节的内容只能来自这一组。**第 2 组是后台分析的中间结果**，只用来支撑"仍需澄清"的列项，**不能搬到"已掌握信息"里**。

---

## 【第 1 组 · 医生告诉你的事实】

### 医生原始描述
{patient_input}

### 前端传来的结构化字段（可能为空）
{structured_hint}

### 结构化复述
{patient_info_text}

---

## 【第 2 组 · 后台分析的中间结果，仅供"仍需澄清"使用】

### 后台基于第 1 组初步推断的疾病名（仅用于检索指南，不是确诊）
{primary_diagnosis}

### 后台匹配到的临床指南
{guideline_name}

### 后台基于指南图谱推断的"患者当前可能位于的路径节点"（**推断结果，不是医生告诉你的事实**，禁止抄进"已掌握信息"）
{matched_nodes_text}

### 后台基于路径计算出的"仍需澄清的判断点"（**核心 — "仍需澄清"章节只能从这里取**）
{missing_dimensions_text}

---

# 现在开始输出 Markdown 正文：
"""
